# استخراج OHLC از تصویر نمودار (Image → Candles)

سه اسکریپت در ریشهٔ مخزن، بدون بیلد و بدون وابستگی شبکه:

| فایل | نقش |
|---|---|
| `chart-ohlc-engine.js` | موتور بینایی (`window.ChartDNACV`) — کالیبراسیون محور قیمت، یافتن شبکهٔ کندل‌ها، اندازه‌گیری بدنه/شدو، اطمینان، CSV، دو رندر روی **قابِ خودِ تصویر** (`renderAnnotated` برای نمای مارک‌دار، `renderReconstructed` برای کندل‌های بازسازی‌شده) و یک رندر مستقلِ خودتنظیم (`renderChart`) |
| `chart-ohlc-extractor.js` | پنجرهٔ «ورود تصویر»: تصویر می‌گیرد، کندل‌ها را از پیکسل اندازه می‌گیرد، خط روندِ بسته‌شونده‌ها را می‌کِشد و عددها را در جدولِ خودش نشان می‌دهد — هیچ دکمهٔ دانلودی ندارد و هیچ فایلی روی دیسک نمی‌نویسد. دکمهٔ اختصاصی ندارد — کلید «ورود تصویر چارت» نوار پخش ( `#btn-import-image` ) همین پنجره را باز می‌کند. با «تأیید»، اندازه‌گیری را به موتور برنامه می‌دهد (یک دیتاست + دو الگو) و چارت کندلیِ همان اندازه‌گیری را روی «محیط الگو» می‌نشاند، ولی جستجو را خودش اجرا نمی‌کند |
| `chart-dna-ui-trim.js` | برداشتن پنجره‌هایی که قرار بود نباشند (از جمله بلوک آپلود وسط «محیط الگو» و کنترل‌های موتور ۲ روی باندل کش‌شدهٔ قدیمی) + جاروکردن دادهٔ باقی‌مانده (پایین همین سند) |

`index.html` هر سه را با `?v=35` بارگذاری می‌کند و `sw.js` آن‌ها را در `PRECACHE_URLS` دارد
(نسخهٔ کش: `chartdna-v2.28.0-no-more-trend-line`). برای انتشار مجدد فقط `?v=` را بالا ببرید.

## اندازهٔ چارت روی «محیط الگو» = اندازهٔ تصویر در پنجرهٔ «ورود تصویر» (v27)

درخواست: «سایز تصویر در محیط الگو دقیقاً همان سایز تصویر در پنجرهٔ ورود تصویر باشد، نه
بزرگ‌تر.» در لحظهٔ «تأیید»، اندازهٔ روی‌صفحه‌ایِ خودِ تصویر داخل پنجره — همان مستطیلِ
object-fit contain داخل بومِ نما — خوانده و به‌عنوان `frame:{w,h}` روی رکورد
`chartdna_px_overlay` ذخیره می‌شود. `paintOverlay()` دیگر کل قابِ کارت را پر نمی‌کند:
چارت دقیقاً در همان `frame` کشیده می‌شود، وسطِ قاب کارت می‌نشیند، اگر کارت کوچک‌تر باشد
با همان نسبت کوچک می‌شود و **هرگز از اندازهٔ پنجره بزرگ‌تر نمی‌شود**
(`k = min(1, stage/frame)`). رکورد قدیمیِ بدون `frame` مثل قبل کل قاب را می‌گیرد تا
چیزی از v26 نشکند.

## موتور ۲ حذف شد؛ «محیط الگو» چارتِ کندلیِ موتور ۳ را نشان می‌دهد (v26)

درخواست: «موتور ۲ باید کلاً با تمام اتصالاتش از ریشه و در هر جا از برنامه حذف شود و
موتور ۱ اطلاعات مورد نیازش را تنها از موتور ۳ دریافت کند. در پنجرهٔ محیط الگو هم نموداری
که از کندل‌های استخراج‌شده توسط موتور ۳ به وجود آمده نمایش داده شود.»

**موتور ۲ چه بود.** استخراج‌گرِ «تصویر ← منحنی الگو» داخل خود باندل React
(`assets/index-DOCOgEgh.js`): تابع `rd()` که از پیکسل‌های تصویرِ کراپ‌شده یک منحنی
~۵۰۰ نقطه‌ای می‌ساخت، OCRِ Tesseract برای خواندن خودکار مقیاس قیمت (`zu()`)، کلیدهای
«استخراج الگو از کادر» و «تراز مقیاس قیمت» در نوار پخش، سربرگ «تشخیص و ثبت الگو از
تصویر» (DNA Vision v2) در تنظیمات با کلید «آپلود تصویر چارت»، میان‌بر «ثبت الگوی جدید از
تصویر» در تب داده‌ها، و کارت تنظیم «تعداد نقاط شباهت و تفکیک الگو».

**در ریشه (خود باندل) چه شد.** هشت پچ کوچک و قابل‌بازبینی روی
`assets/index-DOCOgEgh.js` — سورس این باندل در مخزن نیست، پس همان فایل ویرایش شده و
هر پچ یک درج چندبایتی است، نه بازنویسی:

1. `rd()` در همان خط اول `return null` می‌کند — هستهٔ موتور ۲ دیگر هیچ پیکسلی نمی‌خواند؛
2. `zu()` پیش از رسیدن به Tesseract همان مقیاس پیش‌فرض را برمی‌گرداند —
   `assets/index-BAgSqX26.js` (چانک OCR) دیگر از هیچ مسیری import نمی‌شود؛
3. محتوای سربرگ «تشخیص و ثبت الگو از تصویر» با `!1&&` هرگز render نمی‌شود؛
4. کلیدِ خود سربرگ در تنظیمات `display:none` دارد؛
5. میان‌بر «ثبت الگوی جدید از تصویر» تب داده‌ها هم با `!1&&` هرگز render نمی‌شود؛
6. `onSelectImageClick` — تنها مسیر ورود تصویر به state خود برنامه — پیش از ساختن
   `<input type="file">` برمی‌گردد؛
7. دو کلید «استخراج الگو از کادر» و «تراز مقیاس قیمت» در object استایلِ خودشان
   `display:"none"` و `disabled:!0` دارند؛
8. کارت «تعداد نقاط شباهت و تفکیک الگو» در تنظیماتِ جستجو `display:none` دارد.

**کمربندِ همان پیچ‌ها.** بلوک «engine 2» در `chart-dna-ui-trim.js`: اگر نسخهٔ کش‌شدهٔ
باندلِ قدیم هنوز همان کنترل‌ها را بسازد، همین‌ها از بیرون هم مخفی و `disabled` می‌شوند —
دو کلید با شناسه، دو دکمه با تطبیق دقیق متن، کارت رزولوشن با عنوانش (و صعود محتاطانه تا
نزدیک‌ترین کارت، با گارد `tooBig`). سوئیچ: `chartdna_engine2 = '1'` در localStorage
کنترل‌ها را برمی‌گرداند (فقط روی باندل قدیمی معنا دارد)؛ seamها:
`ChartDnaUiTrim.engine2Pass()` / `.engine2Nodes()` / `.engine2On()` / `.engine2Off()`.

**ورودی موتور ۱ حالا فقط از موتور ۳ می‌آید.** الگوی مرجعِ جستجو دیگر از هیچ تصویری داخل
خود برنامه ساخته نمی‌شود؛ تنها مسیرِ تصویر، پنجرهٔ «ورود تصویر» (موتور ۳) است که با
«تأیید» دیتاست و دو الگو را — مثل v25 — به store و کتابخانهٔ برنامه می‌دهد، و کتابخانهٔ
استاندارد TA-Lib هم مثل قبل سر جای خودش است. دیتاست‌های CSV نمادها (فضای جستجو) به
موتور ۲ ربطی نداشتند و دست نخورده‌اند.

**چارت کندلی روی «محیط الگو».** همان بومِ `#ohlc-trend-overlay` که از v25 فقط خط روند را
می‌کشید، حالا خودِ کندل‌های اندازه‌گیری‌شده را می‌کشد: فتیله و بدنه با دو رنگ خود موتور
(`#26a69a`/`#ef5350`)، چهار خط شبکه با قیمت‌هایشان، خط روند زرد رویشان و کپشن
«بازسازی کندلی از تصویر · n کندل · …». دادهٔ آن (`{candles:[{o,h,l,c,up}], trend}`) با
«تأیید» در `localStorage.chartdna_px_overlay` می‌ماند — عمداً بیرون از الگوهای جاروی
`chart-dna-ui-trim.js` — و در بارگذاری بعد، به‌محض ساخته‌شدن کارت توسط React، دوباره
روی همان قاب می‌نشیند (تلاش کران‌دار: هر ۸۰۰ms تا ۳۰ ثانیه). همان قواعد v25 برقرار است:
گرهی داخل کارتِ React نمی‌رود، `pointer-events:none`، خاموشی با
`chartdna_trend_overlay='0'` یا `ChartDnaOhlc.overlay(false)`؛ و
`ChartDnaOhlc.clearOverlay()` حالا رکورد ذخیره‌شده را هم پاک می‌کند؛ seam تازه:
`ChartDnaOhlc.overlayData()`. تست‌ها: `ui-trim 59`، `ui 137` (بخش 1b و دو سنجهٔ
کندل/رکورد).

## چه کاری انجام می‌دهد (و چه کاری نمی‌کند)

فقط پیکسل‌ها خوانده می‌شود؛ هیچ دادهٔ قیمتی از اینترنت یا «حافظهٔ مدل» نمی‌آید:

1. ماسک رنگی کندل‌های صعودی/نزولی؛ جداکردن اجزای رابط کاربری (تیکر هدر، برچسب قیمت،
   پنل حجم، آیکون‌ها) بر پایهٔ ریخت‌شناسی — نه مستطیل از پیش تعریف‌شده.
2. بازیابی شبکهٔ کندل: جست‌وجوی فاز/گام روی مرکزِ بدنه‌ها + رگرسیون کمترین مربعات،
   رتبه‌بندی با «پوشش یکنواخت روی کل پهنای پلات».
3. کالیبراسیون محور عمودی: خطوط راهنما از اختلاف روشنایی پس‌زمینه (با فیلتر ضد دیتر)،
   اعداد محور با OCR رقمی بدون وابستگی خارجی (قالب ۱۴×۲۰، همبستگی نرمال‌شده)،
   مقایسهٔ مدل خطی و لگاریتمی، و آزمون مستقل با برچسب قیمتِ روی محور.
4. برای هر کندل: بدنه از ردیف‌هایی که تقریباً کل پنجرهٔ ±۲ پیکسلی را پر کرده‌اند،
   High/Low از انتهاهای جزء به هم پیوستهٔ همان کندل (حلقه‌های متصل)، ترمیم ردیف‌های
   پوشیده‌شده توسط خط‌چین قیمت آخر.
5. اطمینان هر کندل از قوانین داخلى (O/H/L/C)، پیوستگی close→open با همسایه، ضخامت بدنه،
   مرز پنل و ابهام رنگ. هر چه اندازه‌گیری نشود **خالی** می‌ماند، نه حدس.

## خط روند، و دادنِ همهٔ اطلاعات استخراج‌شده به موتور (v25)

درخواست: «مسیر رو باز میکنیم و کل اطلاعات استخراج شده به موتور هم داده میشود جهت جستجو»
و «خط روند به دست امده از قیمت‌های بسته شده در محیط الگو رسم شود تا این خط و اطلاعاتش
معیار جستجوی موتور در تمام تایم‌فریم‌ها و تمام نمادها باشد». پس بند «اتصال به برنامه: حذف شد»
در این سند از v25 **برگشته** است؛ آنچه در ادامه می‌آید وضعیت امروز است.

**۱) خط روند.** در `chart-ohlc-engine.js` تابع `fitTrend(values)` اضافه شد: کمترین مربعاتِ
معمولی روی توالی قیمت‌های بسته‌شدهٔ کندل‌هایی که `status:'ok'` دارند (همان‌هایی که موتور
جدول را از آن‌ها می‌سازد؛ کندل ناقص در برازش جایی ندارد). خروجی:
`{ ok, n, slope, intercept, start, end, first, last, risePct, r2, direction, lo, hi, values, closes, model }`.
`direction` فقط همان علامت شیب است — «سیگنال» نیست، توصیفِ همان چندصد کندلِ داخل تصویر.
همچنین `rowForPrice(cal, price)` اضافه شد تا یک قیمت دوباره به ردیف پیکسلی‌اش برگردد
(معکوسِ `calibration.price`)؛ با آن، خط دقیقاً روی همان دو خوانش می‌نشیند که خودِ کندل‌ها
از آن آمده‌اند: `bar.x` برای x و محور قیمتِ خوانده‌شده برای y. نسخهٔ موتور `1.2.0`.

**۲) کجا کشیده می‌شود.** `paintInto()` همان چیزی که می‌کِشد بعد از هر بار رنگ‌کردن،
`drawTrend(cv, box)` را صدا می‌زند؛ پس **هر سه نما** (بازسازی · تصویر اصلی · مارک‌دار) خط را
در قابِ هم‌اندازهٔ خودشان دارند. مختصات بافر (`result.scale`) به قاب ۱۴۰۰px نگاشت می‌شود.
دو برچسب عددی (قیمت اول و آخر خط) فقط روی «تصویر با مارک‌ها» نوشته می‌شود تا «بازسازی»
تمیز بماند و روی خودِ اسکرین‌شات هم چیزی روی متن‌های برنامه ننشیند.

**۳) روی «محیط الگو».** چون آن کارت مال React است، هیچ گره‌ای داخلش نمی‌رود (درسِ
بخش‌های قبلی: بچه‌کردنِ آن کارت با رندر دوبارهٔ React می‌پرید). به‌جایش `#ohlc-trend-overlay`
— یک `<canvas>` با `position:fixed; pointer-events:none; z-index:45` که **هم‌ردیف** کارت به
`body` اضافه می‌شود و از `getBoundingClientRect()` خودِ بومِ کارت جایگیری می‌کند؛ با
`ResizeObserver` + `scroll`/`resize` + یک تایمر ۷۰۰ms (سقف ۶۰ ثانیه) دوباره کشیده می‌شود.
روی آن: خطِ روند زرد، و پشتش با رنگ خاکستری کم‌رنگ همان بسته‌شونده‌هایی که برازش شده‌اند،
و یک کپشن: «خط روند · n کندل · شیب … در هر کندل · r² … · start ← end (…٪)».
خاموش: `localStorage.chartdna_trend_overlay = '0'` یا `ChartDnaOhlc.overlay(false)`.

**۴) چه چیزی به موتور داده می‌شود.** با «تأیید»، `writeExtracted()`:
- یک رکورد در `ChartDNA_Storage / market_datasets` که `ChartDNACV.toDataset` می‌سازد — یعنی
  برای هر کندل `timestamp/open/high/low/close/volume/confidence/notes` (حجم ۰ است چون
  استخراج نمی‌شود؛ `notes` همان یادداشت کیفی هر کندل است) و علاوه بر آن دو فیلد اضافه که
  هیچ چیزی از تصویر را دور نمی‌ریزد: `ds.origin` (تصویر چنده، مقیاس، تعداد کندل، چندتا
  قابل اندازه‌گیری، اطمینان میانگین، `usdPerPx`، مدل محور + معادله + رزیدوال + آراجه‌ها +
  `tagCheck`، گامِ شبکهٔ کندل‌ها، تعداد کندل‌های نیازمند بازبینی) و `ds.trend` (شیب، عرض از
  مبدأ، r²، اول و آخر خط، درصد، جهت، مدل).
- دو ورودی در `chartdna_saved_patterns`: «سری بسته‌شونده‌ها» (`points` = همان close‌ها که
  `Ou` با آن مقایسه می‌کند + `normalizedPoints`) و «خط روند» (`points` = خودِ خط). شناسه‌ها
  `pxrec-…` و `custom_dna_px_…(_trend)` — عمداً بیرونِ الگوی جاروی `chart-dna-ui-trim.js`
  (`img-*` و `/from image|کادر برنامه|از تصویر/i`) تا جاروی قدیمی دادهٔ امروز را نَبَرَد.
- شناسهٔ دیتاست **به** `chartdna_selected_dataset_ids` **اضافه** می‌شود (بازنویسی نمی‌شود).

**۵) سه محدودیتی که پنهان نمی‌کنیم.** (الف) React لیست‌ها را در mount از حافظه می‌خواند، پس
رکوردهای نوشته‌شده **در بارگذاری بعد** در «کتابخانهٔ الگو» و لیست دیتاست‌ها دیده می‌شوند؛
اگر کلید `chartdna_saved_patterns` هنوز ساخته نشده باشد، هر دو الگو در صفِ
`chartdna_px_pending_pattern` (sessionStorage، این‌بار آرایه است تا هر دو بمانند) می‌مانند و
در load بعد الحاق می‌شوند؛ یک «نگاه‌داشتن» سه‌مرحله‌ای (`guardPatterns`) هم اگر React نوشتۀ ما
 را بازنویسی کند، فقط رکورد خودِ ما را برمی‌گرداند. (ب) خطِ صاف برای جستجو یک شکلِ
بازگشتی/دژنه است — با هر بازهٔ یکنواخت نزدیکِ ۱۰۰ امتیاز می‌گیرد — پس یادداشتش همان داخل
کتابخانه این را می‌گوید و برای جستجوی شکل، همان «سری بسته‌شونده‌ها» را انتخاب کنید.
(ج) اجرای جستجو کارِ خودِ کاربر است: ما نه «جستجوی فوری این الگو در تمام نمادها» را فشار
می‌دهیم، نه صفحه را ریلود می‌کنیم، نه چیزی در state برنامه می‌گذاریم.

**کلیدها و خاموش‌کردن‌ها:** `localStorage.chartdna_ohlc_write = '0'` → پنجره دوباره
فقط‌خواندنی می‌شود (خط روند در سه نما و روی محیط الگو همچنان کشیده می‌شود).
`seam`‌ها: `ChartDnaOhlc.trend()`، `.lastWrite()`، `.write()`، `.overlay(bool)`،
`.clearOverlay()`. **برگرداندن این تغییر:** `git checkout 443c445 -- chart-ohlc-engine.js
chart-ohlc-extractor.js chart-dna-ui-trim.js tests/ohlc-image index.html sw.js
OHLC-FROM-IMAGE.md`. تست‌ها: `parity 40`، `ui-trim 49`، `ui 136`.

## اتصال به برنامه: حذف شد

> **به‌روز نشده از v24:** این بخش همان حذف را ثبت می‌کند و از `chartdna-v2.25.0`
> **برگشته** است (بخش بالا را ببینید). آنچه واقعاً حذفِ ماندگار است: دانلود فایل،
> ریلود خودکار، کلیک خودکار «جستجو»، و پنل‌های برچسب/کالیبراسیون/خروجی.


این پنجره از نسخهٔ `chartdna-v2.17.0` **فقط‌خواندنی** است. چهار کنترل آن و کل مسیر
نوشتنشان برداشته شد: «📷 انتخاب تصویر از حافظهٔ دستگاه»، «استفاده از تصویری که در
برنامه آپلود کرده‌اید»، «ثبت به‌عنوان دیتاست Chart DNA» و «ثبت و اجرای جستجو» — با
عملکرد و همهٔ اتصالاتشان:

- `saveDataset(...)` و `openDb()` و نوشتن در `ChartDNA_Storage` / `market_datasets`؛
- `appendPattern`، نرمال‌سازی سری، `norm`، و صفِ `chartdna_ohlc_pending_pattern` در
  sessionStorage (که برای الحاق بی‌خطر به کتابخانهٔ الگوهای برنامه لازم بود)؛
- افزودن شناسه به `chartdna_selected_dataset_ids` و گزینه‌های «الگو اضافه شود» /
  «فقط این دیتاست انتخاب شود» (کل جعبهٔ «اتصال به موتور Chart DNA» از پنل برداشته شد،
  چون فقط همین دو تیک بود)؛
- پرچم `chartdna_ohlc_autosearch`، تابع `reloadPage()`، ریلود خودکار صفحه و کلیک
  خودکار دکمهٔ «جستجو»ی برنامه (بلوک `autoSearch`)؛
- درِ `window.ChartDnaOhlc.saveDataset` و `window.ChartDnaOhlc.reload` هم بسته شد؛
  `useImage`، `run`، `toCSV`، `report`، `open`، `close`، `confirm`، `confirmed`، `deckKey` و
  `deckTakeover` سر جایشان‌اند؛
- `chart-dna-ui-trim.js` اگر نسخهٔ قدیمی در کش کسی هنوز این دکمه‌ها را بسازد، آن‌ها را
  از DOM برمی‌دارد (به `STALE` اضافه شدند، با شمارندهٔ کرانه‌دار).

**دیگر هیچ‌چیز نوشته نمی‌شود.** آخرین چیزی که ما در localStorage می‌گذاشتیم
`chartdna_ohlc_form` بود — همان یادداشت‌های نماد/تایم‌فریم/تاریخِ پنل «برچسب دیتاست» —
و با خودِ پنل حذف شد (بخش «سه پنل کناری» پایین همین صفحه): اطلاعات برچسب فقط برای همان
لحظهٔ کاربر است، در حافظهٔ کوتاه‌مدتِ همان مرحله نگه داشته می‌شود و بعد پاک.
جاروی دادهٔ قدیمیِ ساختهٔ خودِ افزونه (`market_datasets`، `chartdna_saved_patterns`،
`chartdna_ohlc_*`، `chartdna_reference_price`) هم مثل قبل در `chart-dna-ui-trim.js` است و
یک‌بار اجرا می‌شود، پس `chartdna_ohlc_form`ِ نسخهٔ قدیمی هم از حافظه بیرون می‌رود.

**برگرداندن:** `git checkout 7dbf47b -- chart-ohlc-extractor.js` (همراه با بالا بردن
`?v=` در `index.html` و `sw.js`) — همان آخرین نسخه‌ای که دیتاست و الگو می‌نوشت.

### تاریخ و ساعت

تاریخ/ساعت **همیشه خالی** است. پیش‌تر لنگرها را کاربر داخل پنل «برچسب دیتاست» می‌داد
(دو تاریخ، یا تاریخ شروع + ساعت شروع + تایم‌فریم)؛ آن پنل حذف شده، پس منبعی برای
`Date`/`Time` نمانده و ما هم تاریخی نمی‌سازیم. دو ستون در CSV هستند ولی خالی می‌مانند
(ساختار فایل عوض نشده). `assignDates` و پارامتر `extraRefs` تنها در موتور
(`chart-ohlc-engine.js`) و تست برابری (`parity.test.js`) زنده‌اند تا خودِ مسیر
اندازه‌گیری دست‌نخورده بماند؛ هیچ راهی از پنجره به آن‌ها نمی‌رسد.

## اعتبارسنجی

موتور روی همان اسکرین‌شات XAUUSD 1h که با پایتون/NumPy استخراج و بازبینی شده بود اجرا شد
(تست‌های `dev-tests` در محیط توسعه): ۲۹۶ کندل، **۹۹.۶٪** از ۱۱۸۴ سطح OHLC اختلافشان با
خروجی پایتون کمتر از یک پیکسل (≤۰.۶۱ دلار) بود، میانهٔ اختلاف ۰.۲۲ دلار، صفر اختلاف در
جهت کندل، و تاریخ‌ها ۲۹/۲۹۶ یکسان. خطای آزمون مستقلِ برچسب قیمت ۰.۰۱۶ دلار.

## کلید «ورود تصویر چارت» حالا محیط بازسازی OHLC را باز می‌کند

کلیک روی آن کلید (آیکون تصویرِ تنها، `#btn-import-image` داخل ردیف `#remote-control-deck`
زیر `#sidebar-controls`، با عنوان «ورود تصویر چارت») دیگر کاربر را به حافظهٔ دستگاه برای
انتخاب فایل نمی‌برد؛ **محیط بازسازی OHLC از اسکرین‌شات چارت** را باز می‌کند و انتخاب عکس از
حافظه همان‌جا انجام می‌شود. ظاهر کلید، آیکون، نام و markup‌اش اصلاً عوض نشده — فقط مقصدش.

- **چطور جلوی پنجرهٔ حافظه گرفته می‌شود:** در باندل، خودِ handlerِ کلیک یک
  `<input type="file">` می‌سازد و `click()` صدا می‌کند (گرهی که هرگز به DOM اضافه نمی‌شود:
  `onSelectImageClick:()=>{const f=document.createElement("input");… f.click()}`)، پس هیچ
  چیزی برای unbind کردن وجود ندارد. تنها راهِ بسته‌ماندن پنجرهٔ حافظه این است که آن
  handler کلاً اجرا نشود: شنوندهٔ **capture** روی `document` کلیک را می‌خورد
  (`preventDefault` + `stopPropagation` + `stopImmediatePropagation`) پیش از آنکه شنوندهٔ
  delegated خودِ React روی ریشهٔ برنامه آن را ببیند.
- **داخل پنل:** ردیف شش‌کلیدی بالای پنجره (فرمت ردیفِ زیر «محیط الگو» در صفحهٔ اول —
  توضیح کامل در بخش «ردیف شش‌کلیدیِ هم‌فرمتِ نوار پخش»): کلید اول تصویر را از حافظه می‌گیرد
  (یک لمس = `input[type=file]` خودِ پنل؛ رها‌کردن فایل روی همان کلید و Ctrl+V هم کار
  می‌کند)، کلید دوم اندازه‌گیری را اجرا می‌کند، سه کلید بعدی نما را عوض می‌کنند و ششمی
  «تأیید» است. همان‌جا زیر قابِ نماها فقط یک خط وضعیت کوتاه است (بارگذاری تصویر، تعداد
  کندل‌ها، خطا و هشدار) و جدولِ اعداد در کارتِ خودش زیر پنجره. هیچ پنل کناری، هیچ جعبهٔ
  تنظیمی و هیچ دکمهٔ دانلودی نیست — نتیجه فقط روی صفحه خوانده می‌شود.
- **هیچ جای دیگری از برنامه دیگر خودکار اندازه‌گیری نمی‌شود.** شنوندهٔ `FileReader` که در
  نسخهٔ قبل همهٔ انتخاب‌های تصویر را می‌خواند حذف شده است؛ دکمهٔ «انتخاب تصویر» کارتِ برش و
  «آپلود تصویر چارت»ِ سربرگ DNA Vision دقیقاً کار قبلی خودشان را می‌کنند و پنل ما را باز
  نمی‌کنند. تصویرِ اندازه‌گیری‌شده فقط همان است که کاربر در پنل ما انتخاب می‌کند.
- اگر کلیدِ نوار پخش در صفحه نبود (نسخهٔ دیگری از برنامه)، هیچ‌چیز تصاحب نمی‌شود و
  برنامه دست‌نخورده است؛ `window.ChartDnaOhlc.deckKey()` همان کلید را برمی‌گرداند یا
  `null`، و `deckTakeover()` می‌گوید تصاحب فعال است یا نه.
- **خاموش/روشن:** `localStorage.chartdna_deck_takeover = '0'` → آن کلید دوباره فقط کار
  خودش را می‌کند (انتخاب فایل از حافظه) و پنل باز نمی‌شود؛ با پاک‌کردنش تصاحب برمی‌گردد.
- نام پنجره‌ای که باز می‌شود **«ورود تصویر»** است و هیچ توضیحی زیرش نوشته نمی‌شود
  (توضیح قدیمیِ «computer vision…» و برچسب مردهٔ `T.open` از فایل UI برداشته شدند؛
  `margin` زیر عنوان هم به صفر رفت تا فاصلهٔ خالی جایش نماند).
- پنل را می‌توان با `window.ChartDnaOhlc.open()` هم باز کرد؛ بستن با کلید ششمِ ردیف
  («تأیید»)، کلیک روی پس‌زمینه، `Esc` یا `window.ChartDnaOhlc.close()` است. اگر جایی تصویر را به‌صورت برنامه‌ای بدهید، `onPickedImage(dataURL)` همان را
  اندازه می‌گیرد (تست‌ها از همین درِ وارد استفاده می‌کنند).
- اگر نسخهٔ قدیمی در کش کسی هنوز `#ohlc-tool` یا `#ohlc-auto-card` را ساخته باشد،
  `chart-dna-ui-trim.js` آن‌ها را از DOM برمی‌دارد (فهرست `STALE`).

## چه چیزی حذف شد (و چطور برگردانده می‌شود)

این پنجره‌ها مرحله‌به‌مرحله از برنامه برداشته شده‌اند (۱ تا ۳ در
`chartdna-v2.11.0-remove-pattern-panels`، ۴ در `chartdna-v2.12.0`، ۵ در
`chartdna-v2.15.0-crop-upload-removed`):

1. **کارت «کندل‌های بازسازی‌شده از همین تصویر (OHLC از پیکسل)»** — کل
   `chart-ohlc-autopilot.js` حذف شد، یعنی: استخراج خودکار از تصویری که کاربر در
   «محیط الگو» آپلود می‌کند، رسم کندل‌ها در محیط برنامه، ثبت خودکار دیتاست، افزودن
   خودکار الگو به کتابخانه، هم‌گام‌سازی «قیمت مرجع بازار»، دنبال‌کردن کادر زردِ برش و
   بازخوانی/کلیک خودکار «جستجوی فوری» DNA.
2. **پنل «رسم چارت و سطوح تحلیل الگو» (Pattern Chart Overlay) خودِ برنامه** — بومِ
   همپوشانی، خطوط حمایت/مقاومت، تارگت‌ها و برچسب قیمت.
3. **«کارت‌های آماری تحلیل الگو» (Pattern Stats & Targets)** — کارت‌های آماری زیر آن
   (سقف/کف‌ها، سطوح ورود/تارگت/استاپ و متن «آماده»).

   هر دو در باندل مینیفای‌شدهٔ `assets/index-DOCOgEgh.js` هستند و سورسی در این مخزن
   ندارند، پس `chart-dna-ui-trim.js` آن‌ها را **مخفی** می‌کند نه از درخت React حذف
   می‌کند (حذف نودی که React مالکش است ناامن است). لنگر اول، شناسه‌های باثباتِ خودِ
   برنامه است — `#pattern-overlay-canvas-card` و `#pattern-stats-cards-card` — و اگر
   روزی تغییر نام دادند، تطبیقِ **دقیقِ متنِ عنوان** (فارسی یا انگلیسی) به‌عنوان
   پشتیبان کار می‌کند: نزدیک‌ترین والدِ دارای `canvas` یا کلاسِ کارت `display:none` +
   `hidden` می‌شود. اگر برنامه بعداً همان پنل‌ها را دوباره بسازد، دوباره
   مخفی می‌شوند. بقیه دست نمی‌خورد: «خلاصه روند»، «دیتا و الگوها»، هدر، و هیچ‌وقت
      مخفی نمی‌شود (گارد `tooBig`).
4. **دکمهٔ شناور/چسبان خودِ افزونه** (`#ohlc-tool` با `#ohlc-open`) هم حذف شد؛ دیگر
   چیزی به صفحهٔ برنامه اضافه نمی‌شود (بخش بالا را ببینید).
5. **بلوک آپلود در مرکز «محیط الگو» (همهٔ نوشته‌های وسطش، با عملکردشان)** —
   `#image-cropper-card` تا وقتی تصویری داخلش نباشد، یک بومِ وسط‌چین با سه چیز
   نشان می‌دهد: یک جعبهٔ آیکون، «تصویر چارت را بکشید و اینجا رها کنید» و زیرش
   «کادر زرد رنگ را روی بخش مورد نظر از نمودار تنظیم کنید». خودِ همان بلوک
   `onClick` دارد و کارش این است: `P.current.click()` — یعنی همان «کلید آپلود عکس»
   همین بلوک است (دکمهٔ جدا ندارد). در `chart-dna-ui-trim.js`:
   - بلوک `display:none` + `hidden` + `aria-hidden` می‌شود، پس هر سه نوشته از صفحه
     بیرون می‌روند (آیکون داخل همان بلوک است، پس با آن می‌رود)؛
   - عملکردش هم می‌میرد: هر `input[type=file]` داخل کارت `disabled` می‌شود و
     `click()` روی آن بی‌اثر شده (توابع نسخهٔ اصلی برای برگرداندن ذخیره می‌شود)، و
     یک شنوندهٔ capture روی خودِ کارت کلیک‌هایِ رسیده به آن بلوک و همچنین
     `dragover/dragenter/drop` را می‌خورد؛
   - **این کارت هیچ drop handler‌ای در باندل ندارد** (شمارش `onDrop` در
     `assets/index-DOCOgEgh.js` = ۰)؛ یعنی جملهٔ «بکشید و رها کنید» از اول فقط
     متن بوده و تنها مسیر واقعی، همان کلیک بود — که حالا بسته شده؛
   - دست‌نخورده‌ها: بومِ `canvas` و کادر زرد برش، ردیف بالای کارت (عنوان
     «محیط الگو»، «قیمت مرجع»، دکمهٔ «تنظیم») و نوار وضعیت پایین کارت. اگر بعداً
     تصویری از راه دیگری داخل کارت بیاید، همهٔ همان‌ها سر جایشان کار می‌کنند؛
   - `window.ChartDnaUiTrim.cropOff()` / `cropOn()` همین را خاموش/روشن می‌کنند و
     `chartdna_crop_upload = '0'` در localStorage نسخهٔ ماندگاراش است.

   پیامدش را صریح بگوییم: از این پس **هیچ مسیری برای ورود تصویر به خودِ «محیط
   الگو» از داخل همان کارت وجود ندارد** (سربرگ «DNA Vision v2» در تنظیمات یک جریان
   جداست: الگو را در کتابخانه ثبت می‌کند، نه تصویرِ داخل کارت). تصویر برای
   اندازه‌گیری OHLC در پنل خودمان انتخاب می‌شود — همان‌جا که کلید نوار پخش باز می‌کند.
6. **چهار کلیدِ خودِ پنجرهٔ «ورود تصویر» و همهٔ اتصالاتشان** — «📷 انتخاب تصویر از
   حافظهٔ دستگاه»، «استفاده از تصویری که در برنامه آپلود کرده‌اید»، «ثبت به‌عنوان دیتاست
   Chart DNA»، «ثبت و اجرای جستجو»؛ با جعبهٔ «اتصال به موتور Chart DNA» (دو تیک)،
   تابع‌های `saveDataset`/`appendPattern`/`openDb`/`reloadPage` و هندلر درون‌خطی `#ohlc-grab`، درهای
   `ChartDnaOhlc.saveDataset` و `.reload`، و بلوک `autoSearch`. جزئیات و نحوهٔ
   برگرداندن در بخش «اتصال به برنامه: حذف شد» بالا.
7. **سه پنلِ ستون راستِ پنجرهٔ «ورود تصویر»** — «برچسب دیتاست»، «کالیبراسیون محور قیمت»
   و «خروجی» — با کلیدها، ورودی‌ها، ذخیرهٔ localStorage و توابعشان؛ جزئیات در بخش
   «سه پنل کناریِ پنجره حذف شد».
8. **دو کلید «دانلود CSV» و «دانلود تصویر حاشیه‌نویسی‌شده» و جملهٔ راهنمای انتخاب تصویر**
   — با ردیف `.ohlc-actions`، رشته‌های `T.csv`/`T.png`/`T.pickHint`، توابع `dl`/`dlBlob`،
   دو خطِ `disabled` در `run()`، کمکی `deckLabel()` و آن `status(...)` که هنگام باز شدن
   پنجره نوشته می‌شد. جزئیات در بخش «دو کلید دانلود و جملهٔ راهنما حذف شد».

**دادهٔ باقی‌مانده هم یک‌بار جارو می‌شود** (بعد از آن `chartdna_ui_trim_swept`
نوشته می‌شود تا تکرار نشود): دیتاست‌هایی که ما ساخته بودیم (`id` با پیشوند `img-` یا
نامِ «from image»/«کادر برنامه») از `market_datasets`، همان‌ها از
`chartdna_saved_patterns`، همهٔ کلیدهای `chartdna_ohlc_*` از localStorage، برداشتن آن
شناسه‌ها از `chartdna_selected_dataset_ids`، و حذف `chartdna_reference_price` **فقط**
اگر همان عددی باشد که ما نوشته بودیم. دادهٔ خودِ کاربر (دیتاست‌ها و الگوهای دیگر)
دست نمی‌خورد و اگر جایی خوانده نشود، هیچ حذفی انجام نمی‌شود.

**خاموش/روشن:** `localStorage.chartdna_ui_trim = '0'` → کل این اسکریپت هیچ کاری
نمی‌کند. `localStorage.chartdna_crop_upload = '0'` → فقط مورد ۵ (مرکز «محیط الگو»)
به برنامه برگردانده می‌شود. **برگرداندن قابلیت خودکار:**
`git checkout <commit> -- chart-ohlc-autopilot.js` و سپس یک تگ
`<script src="chart-ohlc-autopilot.js?v=N">` به `index.html` و همان فایل به
`PRECACHE_URLS` در `sw.js` (نسخهٔ `d5ce374` آخرین حالت کاملش بود).

ابزار **دستی** سرجایش است: پنل استخراج که با همان کلید «ورود تصویر چارت»
(یا `window.ChartDnaOhlc.open()`) باز می‌شود
(ردیف شش‌کلیدی، قاب سه‌نما، جدول کندل‌ها در کارتِ خودش — بدون هیچ دکمهٔ دانلود)؛ فقط نسخهٔ خودکار و
آن پنجره‌ها حذف شدند (مورد ۵ هم که فقط مخفی‌شدنِ یک بلوک است، هر دو طرفش در تست‌ها
چک می‌شود). دیگر **هیچ دری برای نوشتن در برنامه باز نیست** — نه دکمه‌ای، نه
تابعی روی `window.ChartDnaOhlc`؛ `saveDataset` هم از اوندرورفت.

## ردیف شش‌کلیدیِ هم‌فرمتِ نوار پخش (بالای پنجرهٔ «ورود تصویر»)

`#ohlc-bar` کپیِ همان ردیفی است که در صفحهٔ اول، زیر کارت «محیط الگو» ایستاده
(`#remote-control-deck`): همان ظرف (`flex items-center justify-between gap-1 p-1.5
rounded-xl border`) و روی هر کلید **همان رشتهٔ کلاسِ** `#btn-import-image`:
`flex-1 h-9 rounded-lg flex items-center justify-center transition-all duration-150
border cursor-pointer active:scale-95 group relative`. همان‌جا هم کلیدها فقط آیکون‌اند؛
پس اینجا هم **فقط آیکون، بدون نوشته** — و برای اینکه ظاهر به Tailwind وابسته نماند،
همان اعداد (۳۶px قد، گوشهٔ ۸px، فاصلهٔ ۴px) در CSS خودِ پنل هم نوشته شده‌اند.

آیکون‌ها هم از همان کتابخانه‌اند: **lucide-react v0.475.0** — دادهٔ گره‌های هر گلیف از
خودِ باندل برنامه برداشته و به‌صورت SVG درون‌خطی شده (`<svg class="lucide lucide-…"
viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">`)، با همان
`w-4 h-4 group-hover:scale-110 transition-transform` که کلیدهای برنامه دارند:

| # | آیکون (lucide) | کلید | کار |
|---|---|---|---|
| ۱ | `image` | `#ohlc-drop` | انتخاب تصویر / رها‌کردن فایل / Ctrl+V |
| ۲ | `scan-line` | `#ohlc-run` | «استخراج کندل‌ها» روی تصویرِ انتخاب‌شده |
| ۳ | `chart-no-axes-column` | `[data-view=chart]` | کندل‌های بازسازی‌شده (همان قاب، همان جای کندل‌ها) |
| ۴ | `eye` | `[data-view=orig]` | اسکرین‌شات، بدون هیچ علامتی |
| ۵ | `crosshair` | `[data-view=ann]` | همان اسکرین‌شات + کادرهای شماره‌گذاری‌شده، خط‌های کالیبراسیون… |
| ۶ | `circle-check` | `#ohlc-confirm` | **تأیید** و بازگشت به صفحهٔ اصلی برنامه |

- **هیچ متنی روی کلیدها نیست**، ولی هیچ جمله‌ای هم دور ریخته نشده: هر کلید همان جمله را در
  `title` و `aria-label` دارد (تولtip در دسکتاپ، و صفحه‌خوان‌ها هم همان را می‌خوانند).
  `#ohlc-file` هم عمداً بیرونِ ردیف و به‌عنوان خواهرِ آن نشسته تا داخل دکمه input نباشد.
- **`کلید ششم = تأیید`:** تا وقتی چیزی استخراج نشده `disabled` است (همان کاری که کلیدهای
  خودِ برنامه با `disabled:!g` می‌کنند). یک فشارش، روند را تأیید‌شده علامت می‌زند
  (`state.confirmedKey` + `confirmedAt`/`confirmedCandles` در `window.__ohlcReport`) و
  پنجره را می‌بندد؛ پشتِ همین پنجره صفحهٔ اول برنامه است، پس کاربر همان‌جا می‌ایستد.
  **هیچ نوشتنی در برنامه انجام نمی‌شود** — تست `3b` همان‌جا `IndexedDB opens=0` و
  `search clicks=0` را می‌سنجد. نتیجهٔ استخراج و جدول سرِ جایشان می‌مانند و با
  `ChartDnaOhlc.open()` دوباره باز می‌شوند.
- **`بستن` از پایین کارت حذف شد** (کلید و رشته‌اش با هم) — همان کلید حالا ششمین کلیدِ این
  ردیف است با نام «تأیید». پنجره همچنان با Escape یا کلیک روی پس‌زمینهٔ تیره بسته می‌شود،
  و `ChartDnaOhlc.close()` هم کنار `open()` اضافه شد.
- **مکانیزم «دو خط / فقط آیکون» نسخهٔ قبل برداشته شد** (`fitBar`، `.ohlc-lbl`،
  `ohlc-1/ohlc-2/ohlc-ic`، `ResizeObserver`ِ بازسنجی): وقتی کلیدها از اول آیکون‌اند،
  اندازه‌گیریِ جا‌شدنِ متن معنایش را از دست می‌دهد. این‌ها حذف شدند، نه مخفی — و `.ohlc-primary`
  و `.ohlc-danger` هم از CSS رفتند، چون تنها مصرف‌کننده‌هایشان همان کلیدهای «استخراج» مربعی و
  «بستن» بودند.

## یک قاب برای هر سه نما

قبلاً کادر «کندل‌های بازسازی‌شده» یک بوم ۱۰۰۰×۳۰۰ بود (در CSS هم `height:300px` قفل شده)
و «تصویر اصلی» و «تصویر با مارک‌ها» هر کدام اندازه‌ای از خودشان داشتند — برای همین
درازا و قد این نماها با هم نمی‌خواند. حالا **یک قاب** مشترک است:

- `frameBox()` در `chart-ohlc-extractor.js` قاب را از خودِ اسکرین‌شات می‌گیرد و ضلع بلندش
  را حداکثر ۱۴۰۰ پیکسل می‌کند (مثلاً ۲۷۱۲×۱۲۲۰ ← ۱۴۰۰×۶۳۰).
- `paintInto()` همان قاب را به هر سه بوم می‌دهد: `#ohlc-chart`، `#ohlc-orig`، `#ohlc-ann` —
  پس عرض، ارتفاع و نسبتِ هر سه **یکی** است و هیچ‌کدام کشیده نمی‌شود
  (یک قاعدهٔ CSS برای هر سه: `width:100%;height:auto`).
- `renderReconstructed` کندل‌های بازسازی‌شده را در **فضای پیکسلی همان تصویر** می‌کِشد:
  هر کندل دقیقاً روی ستون و ردیفی که اندازه گرفته شده می‌نشیند (`b.x`, `rowHigh/rowLow`,
  `rowBodyTop/rowBodyBot`, پهنای بدنه از گام شبکه) — بدون auto-fit و بدون مقیاس‌ مجدد؛
  کندل‌های خوانده‌نشده مثل نمای مارک‌دار با کادر قرمز علامت می‌خورند و رنگ
  صعودی/نزولی همان تیل و قرمز همیشگی است.
- نتیجه: اگر سه نما را یکی‌یکی عوض کنید، مجموعهٔ کندل‌ها جا عوض نمی‌کند — شباهت (و
  اختلاف) چشمی قابل مقایسه است. تست `ui` این را با پیکسل می‌سنجد: ۲۵۷/۲۵۷ کندلِ با‌اطمینان
  در نمای مارک‌دار، دقیقاً روی همان پیکسلی علامت دارند که نمای بازسازی‌شده شدو کشیده است.
- **ریتم کارت‌ها**: در پهنای ≥۸۲۰px، کادرِ هر سه نما `min-height:340px` و `max-height:390px`
  می‌گیرد — همان ارتفاعی که خودِ برنامه به کارت‌های چارتش می‌دهد (`h-[340px] md:h-[390px]` روی
  `#comparative-chart-card` و `#pattern-overlay-canvas-card`). چون ما را هم با همان
  `object-fit:contain` تنظیم کردیم، ارتفاع کارت‌ها در صفحه یک‌دست است و تصویر هم کشیده نمی‌شود.
  این را روی **گره‌های خودمان** انجام دادیم، نه با استایل دادن به کارت‌های React: آن کارت‌ها
  خودشان با `ResizeObserver` بومشان را به اندازهٔ کادر می‌سازند و اگر نسبتِ ما را به آن‌ها
  تحمیل می‌کردیم، یا با resize خودشان می‌جنگیدیم یا شکلِ نمودارِ خود برنامه عوض می‌شد.
- چون تصویر با `object-fit:contain` وسط کادر نشسته، کلیکِ «افزودن نقطهٔ مرجع روی محور قیمت»
  هم روی **خودِ تصویر** نگاشت می‌شود (`contentBox()`): اگر انگشت روی حاشیهٔ خالیِ کادر بخورد،
  پنجره می‌گوید «روی خودِ تصویر کلیک کنید» و ردیفی از هیچ می‌سازد **نیست**.
- قابِ نمایش ۱۴۰۰px است تا گوشی سه بوم تمام‌رزولوشن نگه ندارد، ولی **فایل PNG** هنگام
  دانلود دوباره در رزولوشن کاملِ تصویر رندر می‌شود (۲۷۱۲×۱۲۲۰) — یعنی هم‌اندازه‌کردن نماها
  به قیمت افتِ کیفیت فایل تمام نمی‌شود.

## سه پنل کناریِ پنجره حذف شد (برچسب دیتاست · کالیبراسیون · خروجی)

پنجرهٔ «ورود تصویر» دو ستون بود: قاب نماها در چپ، و در راست سه جعبه —
**«برچسب دیتاست»** (نماد، تایم‌فریم، تاریخ شروع/پایان، ساعت شروع)،
**«کالیبراسیون محور قیمت»** (متن راهنما، ردیف و قیمت مرجع، «افزودن»، «پاک کردن»، فهرست
مراجع) و **«خروجی»** (گزارش چندخطی زیر تیترش). هر سه با کلیدهایشان حذف شدند —
نه مخفی‌سازی: چیزی که ساخته نمی‌شود، جارو هم نمی‌خواهد.

**چه رفت (در `chart-ohlc-extractor.js`، نسخهٔ API = ۱۴):**
- مارک‌آپ: `#ohlc-symbol`، `#ohlc-tf`، `#ohlc-d0`، `#ohlc-d1`، `#ohlc-t0`، `#ohlc-ref-row`،
  `#ohlc-ref-price`، `#ohlc-ref-add`، `#ohlc-ref-clear`، `#ohlc-points` و هر سه
  `<div class="ohlc-box">`؛
- حالت: `state.points` از `state` بیرون رفت؛ `renderPoints`؛ و کلیک روی تصویر اصلی برای
  گرفتن «ردیف مرجع» (با نگاشت `contentBox` به نوار letterbox) — آن کلیک دیگر هیچ نقطه‌ای
  جمع نمی‌کند؛
- ذخیره‌سازی: `FORM`، `restoreForm`، شنونده‌های `change` و کلید `chartdna_ohlc_form` —
  هیچ ورودی‌ای نمانده که ذخیره‌اش کنند؛
- مسیرها: `extraRefs` از `run()` حذف شد (موتور نبودنش را می‌پذیرد)، تابع `applyDates` با
  کل بدنه‌اش رفت، و `report()` دیگر `source: {symbol, timeframe}` نمی‌نویسد؛
- نوشته‌ها: `T.calibNote`؛ استایل‌ها: `.ohlc-grid`، `.ohlc-box` و فرزندهایش،
  `.ohlc-refrow`، قواعد `#ohlc-points …`، `.ohlc-kv`، `.ohlc-muted` و
  `@media(max-width:820px){.ohlc-grid…}`. ستون‌بندی دو‌تایی جایش را به یک ستون تمام‌عرض
  داد، پس قاب نماها دیگر عرضش را با هیچ پنل دیگری تقسیم نمی‌کند؛
- `#ohlc-status` تنها بازماندهٔ آن ستون است و جعبه و تیتر «خروجی» را ندارد: یک خط کوتاه
  زیر جدول با گزارش فشرده (تعداد کندل‌ها، مدل محور، ۱ پیکسل ≈ چند دلار، چند کندل نیازمند
  بازبینی، و خطا/هشدار). متن کامل جزئیات همچنان در `window.__ohlcReport` است — در
  حافظه، برای تست و دیباگ، نه روی صفحه.

**پیامدها، صریح:**
- `Date`/`Time` در CSV همیشه خالی است (بخش «تاریخ و ساعت»)؛
- **زنجیرهٔ پشتیبانِ دستیِ محور رفت:** اگر خواندن خودکار محور قیمت شکست بخورد، دیگر نمی‌توان
  ردیف/قیمت مرجع دستی داد. پنجره همان جملهٔ «محور قیمت خوانده نشد → Open/High/Low/Close
  خالی می‌ماند» را نشان می‌دهد و اعداد را **نمی‌سازد** — قاعدهٔ همیشگی: چیزی که در
  پیکسل‌ها نیست، در CSV نیست؛
- نام فایل CSV از «نماد» به `chart_ohlc_from_image.csv` عوض شد (نام‌گذاری از فیلد
  نماد می‌آمد)؛
- خط‌های ترازِ کالیبراسی روی «تصویر با مارک‌ها» دست‌نخورده‌اند: از `result.calibration`
  خوانده می‌شوند، نه از ورودی کاربر.

**جاروی نسخهٔ قدیمی:** `chart-dna-ui-trim.js` (نسخهٔ ۴) همان ده شناسه را به `STALE` اضافه
کرد تا اگر اسکریپت قدیمی در کش کسی هنوز آن جعبه‌ها را می‌سازد، از صفحه برداشته شوند
(کرانهٔ هر شناسه ۵ بار)؛ `chartdna_ohlc_form`ِ باقی‌مانده هم در جاروی داده پاک می‌شود.

**برگرداندن:** `git checkout 920c5e1 -- chart-ohlc-extractor.js` و پایین بردن `?v=` در
`index.html` و `sw.js` (و `version: 3` در `chart-dna-ui-trim.js`).

## جدول کندل‌ها، در پنجرهٔ خودش زیرِ پنجرهٔ «ورود تصویر»

پیش‌تر جدول داخل همان کارتِ پنجره بود و دو ستونِ شلوغ داشت. حالا:

- **کارت دوم، زیر کارت اول، با همان عرض و همان قاب:** `#ohlc-modal` یک ستونِ فلکس است
  (`flex-direction:column;align-items:center`) و دو فرزند دارد — `#ohlc-card` (ردیف کلیدها،
  قاب نماها، خط وضعیت، دکمهٔ دانلودها) و `#ohlc-table-card`. هر دو یک قاعدهٔ عرض و قاب دارند:
  `width:min(1080px,97vw)` با همان `border:1px solid #334155`، `border-radius:18px`،
  `padding:16px` و `box-sizing:border-box`؛ فاصله‌شان `12px`. تا چیزی استخراج نشده کارت جدول
  `ohlc-hidden` است تا صفحه جای خالی نداشته باشد؛ با اولین نتیجه باز می‌شود و با انتخاب
  تصویرِ تازه دوباره خالی و پنهان می‌شود (جدولِ قدیمی زیر پنجره معلق نمی‌ماند).
- **ستون `note` حذف شد.** یادداشت‌های کیفیت (`b.notes`) جایی را در جدول اشغال نمی‌کنند؛
  همچنان در `window.__ohlcReport.bars` و در `toDataset`ِ موتور هست‌اند. CSV هم مثل قبل نه
  ستونِ ثابتِ خودش را دارد (`Candle,Date,Time,Open,High,Low,Close,Direction,Confidence`) —
  یادداشت هیچ‌وقت در CSV نبود، پس فایل عوض نشده. سرِ جدول حالا هشت خانه است:
  `# · Date · O · H · L · C · Dir · Conf`.
- **`Dir` به‌جای کلمه، فلش است:** `Bullish` → `<span class="ohlc-up">↑</span>` سبز
  (`#34d399`)، `Bearish` → `<span class="ohlc-down">↓</span>` قرمز (`#f87171`)، و اگر جهت از
  پیکسل‌ها خوانده نشده باشد `·` خاکستری. در خودِ خانه هیچ متنی نیست؛ کلمه در `title` و
  `aria-label` همان `span` مانده، پس با نشانگر و صفحه‌خوان هنوز خوانده می‌شود. خانه
  وسط‌چین و `nowrap` است تا ستون کوتاه بماند.
- **جدول در یک قابِ اسکرول با سرِ چسبان است:** `#ohlc-table{max-height:min(46vh,420px);
  overflow:auto}` و `thead th{position:sticky;top:0}` — همان پیش‌نمایش ۱۹سطری (۱۴ اول + `…` +
  ۴ آخر) ارتفاع صفحه را نمی‌بلعد و سرِ ستون‌ها موقع اسکرول می‌ماند؛ به‌همین دلیل
  `border-collapse:separate` شد (با `collapse` خطِ سرِ چسبان از دست می‌رود).

تست‌ها (`tests/ohlc-image/ui.test.js`، بخش ۱ و ۲): دو کارتِ هم‌عرض و مودالِ ستونی، جدولِ خالی
و پنهان تا پیش از استخراج، `th.length === 8` و نبودِ `note`، فلشِ داخل `td.ohlc-dir` و
باقی‌ماندنِ کلمه در `title`/`aria-label`، نبودِ هیچ متنِ یادداشتی در جدول، پنهان‌شدنِ کارت با
تصویرِ تازه و بازگشتش با نتیجهٔ بعد. `OHLC_PREVIEW=/path node tests/ohlc-image/ui.test.js`
همان HTMLِ واقعی را در یک فایل می‌نویسد (پیش‌نمایشِ دیدنی، نه دادهٔ تازه).

## دو کلید دانلود و جملهٔ راهنما حذف شد

در `chart-ohlc-extractor.js` (نسخهٔ API = ۱۶):

- **ردیف دانلود از مارک‌آپ بیرون رفت:** `<div class="ohlc-actions">` با دو دکمهٔ
  `#ohlc-csv` («دانلود CSV») و `#ohlc-png` («دانلود تصویر حاشیه‌نویسی‌شده»)، رشته‌های
  `T.csv`/`T.png`، قواعد `.ohlc-actions`/`.ohlc-actions button`/`.ohlc-secondary` و دو خطِ
  `['ohlc-csv','ohlc-png'].forEach(… disabled …)` در `run()`.
- **عملکردش هم حذف شد، نه خاموش:** شنونده‌های `click` هر دو کلید، `dl()` و `dlBlob()` — تنها
  جایی در کل افزونه که `Blob` می‌ساخت، `<a download>` می‌داد و `createObjectURL` صدا می‌زد.
  بعد از این تغییر **هیچ مسیری برای نوشتن فایل در این صفحه نیست**؛ تست
  `the page wrote no file at all` با ماک‌های `Blob` و `createElement('a')` همان را می‌سنجد
  (`blobs=0 · download=null`).
- **جملهٔ راهنما هم رفت:** `T.pickHint` («تصویر اسکرین‌شات چارت را از حافظه انتخاب کنید») و
  دنبالهٔ «— اینجا همان کاری است که کلید «ورود تصویر چارت» به آن می‌آید» که `status(...)`
  هنگام باز شدن پنجره با کلید نوار پخش می‌نوشت. تنها مصرف‌کنندهٔ آن رشته‌ها همان خط بود، پس
  `deckLabel()` هم حذف شد. کارت حالا با همان خطِ بی‌کارش باز می‌شود («هنوز تصویری انتخاب
  نشده.») و اگر قبلاً نتیجه‌ای گرفته باشد همان می‌ماند — سازگار با اینکه تصویر و اعداد بعد
  از «تأیید» و باز کردنِ دوباره سر جایشان‌اند.
- **دست‌نخورده‌ها:** نام و tooltip خودِ کلید آپلود (`DROP_HINT`: «تصویر نمودار را اینجا رها
  کنید…») — آن جملهٔ کلید است نه پنل؛ موتور `chart-ohlc-engine.js` با `toCSV` و
  `renderAnnotated` (نقاشیِ نمای «تصویر با مارک‌ها» همان‌جا است)؛ و درِ حافظه‌ای
  `window.ChartDnaOhlc.toCSV()` که رشتهٔ CSV را با همان نه ستونِ قرارِ استخراج برمی‌گرداند.
  **اگر این درِ آخر را هم نمی‌خواهی، بگو تا ببندمش** — فعلاً نگه داشته‌ایم تا عددی که روی
  صفحه است از برنامه قابل‌گرفتن بماند، بی‌آنکه فایلی نوشته شود.
- CSVِ تحویلِ Task 1 (`/home/user/xauusd_1h_ohlc_from_image.csv`، ۲۹۶ سطر) اثری آفلاین از
  خودِ استخراج است و به این دکمه‌ها ربطی نداشت؛ سر جایش می‌ماند.

**پیامد، صریح:** کاربر از داخل پنجره دیگر نمی‌تواند CSV یا PNG بگیرد؛ اعداد فقط در جدولِ زیر
پنجره و در `window.__ohlcReport` هستند. مسیرِ تغذیهٔ دیتاستِ برنامه (مورد «ج») مثل قبل رد است،
و حالا مسیر خروجیِ فایل هم بسته شده.

**جاروی نسخهٔ قدیمی:** `chart-dna-ui-trim.js` (نسخهٔ ۵) `ohlc-csv` و `ohlc-png` را به `STALE`
اضافه کرد تا اگر بیلد قدیمیِ در کش کسی هنوز آن ردیف را بسازد، دو کلیدش از صفحه برداشته
شوند (کرانهٔ هر شناسه ۵ بار).

**برگرداندن:** `git checkout f9c4538 -- chart-ohlc-extractor.js chart-dna-ui-trim.js` و پایین
آوردن `?v=` در `index.html` و `sw.js` (به `?v=23`).

## محدودیت‌ها

خط روندِ این پنجره یک برازش روی کندل‌های **همان تصویر** است: نه پیش‌بینی، نه تشخیص
روندنمای چندتایم‌فریمی. رکوردهایی که به موتور داده می‌شوند در بارگذاریِ بعد در برنامه ظاهر
می‌شوند (بالای همین سند) و جستجو را خودِ کاربر اجرا می‌کند.
دقت به گام ۱ پیکسل محدود است (این چارت: ≈۰.۵۵ دلار بر پیکسل)؛ در نواحی شلوغ که بدنه‌ها
روی هم می‌افتند مرزها فقط تا دقت همان پنجره قابل تفکیک‌اند؛ حجم استخراج نمی‌شود؛
برچسب‌های ساعتِ تک‌تک کندل‌ها در این سبک چارت وجود ندارند؛ خروجی یک بازسازی هندسی است،
نه تحلیل تکنیکال. هم‌اندازه‌سازی شامل سه نمای همین پنجره است — بومِ چارتِ خودِ برنامه
(کارت‌های React، مثل «کشف الگو در تاریخچه») را ما نمی‌کِشیم و دست به استایلش نمی‌بریم؛
اگر هم‌قاب‌کردن آن هم خواسته شود، تصمیم جداگانه‌اش لازم است.

## v28 — خط روند حذف شد؛ «محیط الگو» هم‌شکل پنجرهٔ تصویر

**چه تغییر کرد**

1. **خط روند زرد به‌کلی حذف شد — از هر دو پنجره و از تمام محاسبات.**
   - `chart-ohlc-engine.js` → `1.3.0`: توابع `fitTrend` و `rowForPrice` (برازش کمترین‌مربعات و معکوس قیمت→ردیف) پاک شدند؛ دیگر هیچ‌جای برنامه چنین محاسبه‌ای ندارد.
   - `chart-ohlc-extractor.js` → `version: 19`: `state.trend`، `drawTrend`، فیلدهای `trend`/`confirmedTrend` در گزارش، `ds.trend` روی دیتاست، الگوی دومِ `…_trend` در کتابخانه، و seam `trend()` همگی حذف شدند. تأیید هر تصویر حالا **یک** الگو می‌نویسد: بسته‌شونده‌ها (`custom_dna_px_<id>`، دستهٔ `Pixel closes`).
   - رکورد overlay (`chartdna_px_overlay`) فقط `{at, frame, candles}` دارد؛ چارت روی «محیط الگو» فقط کندل‌هاست با برچسب «بازسازی کندلی از تصویر · n کندل».
   - **جاروی یک‌باره:** ورودی‌های قدیمیِ `custom_dna_px_*_trend` در `chartdna_saved_patterns` (و صفِ در انتظار) یک بار پاک می‌شوند؛ پرچم `chartdna_px_trend_swept` از تکرار جلوگیری می‌کند. الگوهای خود کاربر دست نمی‌خورند.
   - نام بومِ `ohlc-trend-overlay` و کلید `chartdna_trend_overlay` عمداً همان ماند (تغییر نام، کلید خاموشیِ ذخیره‌شدهٔ کاربران و تست‌ها را می‌شکست).

2. **«محیط الگو» دقیقاً هم‌شکل پنجرهٔ «ورود تصویر» می‌شود.**
   - وقتی overlay با `frame` رنگ می‌شود، استیجِ داخل کارت (div مالِ React؛ فقط استایل inline می‌گیرد، نه حذف) ارتفاعش `round(min(عرض استیج, frame.w) × frame.h ÷ frame.w)` می‌شود با `minHeight:0` — یعنی همان نوار پهنِ پنجرهٔ تصویر، نه مربع.
   - مقدارهای قبلی inline نگه داشته می‌شوند و با خاموش‌شدن overlay (`overlay(false)` یا `clearOverlay()`) برمی‌گردند (`unshapeStage`).
   - رکوردهای قدیمیِ بدون `frame` (پیش از v27) مثل قبل کل استیج را پر می‌کنند و شکل کارت را عوض نمی‌کنند — یک بار تأییدِ دوبارهٔ تصویر، رکورد را با `frame` می‌نویسد و از آن به بعد هم‌شکل می‌ماند.

**نسخه‌ها:** `?v=35`، `sw.js` → `chartdna-v2.28.0-no-more-trend-line`.

**تست‌ها:** smoke ‏30/30 (بدون پیکسل زرد، جاروی `_trend`، هم‌شکل‌سازی استیج 600→200 برای قاب 1200×400 و بازگشت استایل)، ‏ui-trim ‏59/59 (انتظار جدید: `_trend` جارو شده + پرچم)، ‏ui.test.js به‌روز شد (۱۳۲ سنجه؛ اجرا نیازمند فیکسچر `OHLC_IMG`)، parity بدون فیکسچر اجرا نمی‌شود.

**برگشت:** `git revert <این کامیت>`؛ v27 = `git revert 3b6b54b`.

## v29 — کادرِ مادر (خود کارت «محیط الگو») هم به اندازهٔ چارت جمع می‌شود

**مشکل v28:** استیجِ داخل کارت هم‌شکل قاب می‌شد، اما خود کارت در باندل ارتفاع ثابت دارد
(`min-h-[300px] h-[330px] md:h-[360px]`) و استیج هم `flex-1` است؛ نتیجه: زیر چارت نوار خالی می‌ماند.

**راه‌حل (`shapeStage` در `chart-ohlc-extractor.js`):**
- استیج: `flex: 0 0 auto` + `height/maxHeight` به نسبت قاب و `minHeight: 0` — دیگر با flex کش نمی‌آید.
- کارت: `height: auto` و `minHeight: 0` به‌صورت inline (بر کلاس‌های Tailwind غلبه می‌کند) — کارت محتوایش را در آغوش می‌گیرد و فضای خالی نمی‌ماند.
- مقدارهای قبلیِ inline هر دو (استیج و کارت) نگه داشته و با خاموش‌شدن overlay در `unshapeStage` برگردانده می‌شوند.
- رکوردهای بدون `frame` مثل قبل: نه استیج و نه کارت دست نمی‌خورند.

**نسخه‌ها:** `?v=35`، `sw.js` → `chartdna-v2.29.0-card-hugs-the-chart`.

**تست‌ها:** smoke ‏31/31 — سنجهٔ جدید با همان کلاس‌های واقعی باندل (کارت `h-[330px]` + استیج `relative flex-1`): استیج 600→200 با `flex:0 0 auto`، کارت `height:auto`، و بازگشت کامل هر دو پس از خاموشی. ‏ui-trim ‏59/59.

**برگشت:** `git revert <این کامیت>`؛ v28 = `git revert 0a04178`.

## v30 — چارت به کارت برگشت (رفع خالی‌ماندن «محیط الگو» در v29)

**مشکل:** paintOverlay اندازه را از «اولین canvas داخل کارت» می‌خواند. وقتی تصویری در کارت
بارگذاری نشده، آن canvas مخفی است و 0×0 برمی‌گردد → چارت پنهان می‌شد؛ ولی هم‌شکل‌سازیِ
کارت قبل از آن انجام شده بود، پس کارت کوچک می‌شد و خالی می‌ماند.

**راه‌حل:** فهرست نامزدها (canvasها → استیج → خود کارت) پیموده می‌شود و **اولین جعبه‌ای که
واقعاً روی صفحه اندازه دارد** (w>60 و h>40) ملاک نشستن چارت است؛ canvas مخفی دیگر چارت را
ساکت نمی‌کند. بوتِ بازنشانی هم از 800ms به 150ms کوتاه شد تا پرش «اول بزرگ، بعد کوچک» کارت
در رفرش تا حد ممکن کوتاه باشد (حذف کاملش دست ما نیست: React اول کارت را با ارتفاع خودش
می‌سازد، بعد اسکریپت ما می‌رسد).

**نسخه‌ها:** `?v=35`، `sw.js` → `chartdna-v2.30.0-chart-back-on-the-card`.

**تست‌ها:** smoke ‏32/32 — سنجهٔ جدید عین باگ: canvas مخفی 0×0 جلوی استیج؛ چارت باید روی
استیج بنشیند (600×200@40,90). ‏ui-trim ‏59/59.

**برگشت:** `git revert <این کامیت>`؛ v29 = `git revert 711b3f9`.

## v31 — «محیط الگو» از لحظهٔ باز شدن برنامه هم‌قابِ پنجرهٔ تصویر است

**خواسته:** کادر «محیط الگو» چه در بدو ورود و چه وقتی چارت رویش می‌نشیند باید همان
شکل/اندازهٔ پنجرهٔ «ورود تصویر» باشد — نه فقط بعد از رسم چارت.

**راه‌حل:**
- قابِ پنجره جدا از رکورد چارت در `chartdna_px_frame` نگه داشته می‌شود (در هر «تأیید» نوشته
  می‌شود و با پاک‌شدن چارت از بین نمی‌رود).
- `shapeCardFromBoot`: به محض ساخته‌شدن کارت توسط React (چرخهٔ 150ms)، کارت با همان قاب
  هم‌شکل می‌شود — حتی وقتی هیچ چارتی نیست؛ بعد یک نگهبان سبک (ResizeObserver + چرخهٔ 1.2s
  که وقتی شکل برقرار است هیچ کاری نمی‌کند) شکل را بعد از هر بازساخت React برمی‌گرداند.
- `clearOverlay` فقط چارت و رکوردش را پاک می‌کند؛ شکل کارت می‌ماند. کلید خاموشی
  `chartdna_trend_overlay='0'` همه‌چیز — چارت و شکل — را به حالت خود برنامه برمی‌گرداند.
- seam جدید: `ChartDnaOhlc.frame()`.

**نسخه‌ها:** `?v=35`، `sw.js` → `chartdna-v2.31.0-window-shaped-from-boot`.

**تست‌ها:** smoke ‏35/35 — سه سنجهٔ جدید: هم‌شکل‌سازی در بدو ورود فقط با قاب (بدون رکورد چارت)،
خواندن قاب از seam، و ماندن شکل بعد از `clearOverlay`. ‏ui-trim ‏59/59.

**برگشت:** `git revert <این کامیت>`؛ v30 = `git revert d2a9a35`.

## v32 — حذف نشان «pts» و «قیمت مرجع» با تمام اتصالاتشان

هر دو از بازمانده‌های موتور ۲ بودند.

**۱) قطع در ریشهٔ باندل (`assets/index-DOCOgEgh.js` — وصله‌های P9–P11):**
- **P9 (×۵):** `referencePrice:Ce` → `referencePrice:0` — هر ۵ مصرف‌کننده (کارت برش، تحلیل الگو،
  اطلاعات الگوی کشف‌شده، جدول نتایج، مودال نمودار کندلی) دیگر قیمت مرجعی نمی‌بینند؛ همهٔ
  محاسبات وابسته («قیمت هدف بر مبنای مرجع»، مبالغ سقف/کف آینده و تولتیپ‌ها) با شرط `>0`
  خودشان حذف می‌شوند.
- **P10 (×۳) و P11 (×۱):** `onUpdateReferencePrice` → تابع پوچ — هیچ‌جای برنامه دیگر نمی‌تواند
  قیمت مرجع بنویسد.

**۲) پنهان‌سازی آنچه React هنوز می‌کشد (`chart-dna-ui-trim.js` → نسخهٔ ۷، بخش `killRefPts`):**
- نشان «pts» (چیپ آبی `bg-cyan-950`) روی «محیط الگو» و «اطلاعات الگوی کشف شده» — برچسب‌های
  pts کتابخانه (طول واقعی الگوها؛ دادهٔ کاربر) دست نمی‌خورند.
- ردیف «قیمت مرجع: … تنظیم» در سر کارت و هر کلید «تنظیم/Edit» با عنوان قیمت مرجع در کل برنامه؛
  اگر دورگیرش امن باشد (بدون input فایل) یک‌جا، وگرنه تک‌به‌تک.
- کلید خاموشی: `chartdna_refpts='1'`؛ seamها: `refptsNodes/refptsPass/refptsOn/refptsOff`.

**نسخه‌ها:** `?v=35`، `sw.js` → `chartdna-v2.32.0-no-pts-no-refprice`.

**تست‌ها:** smoke ‏41/41 (شمارش دقیق P9–P11 + پنهان‌شدن دو نشان pts و ردیف قیمت مرجع + مصون
ماندن pts کتابخانه + رفت‌وبرگشت seam)، ‏ui-trim ‏62/62 (سه سنجهٔ جدید).

**برگشت:** `git revert <این کامیت>`؛ v31 = `git revert e9f49d6`.

## v33 — کلید سبز «تأیید» روی پنجرهٔ خالی هم کار می‌کند

**خواسته:** حتی وقتی تصویری در «ورود تصویر» نیست و داده‌ای وجود ندارد، کلید سبز پنجره را
خالی ببندد و به صفحهٔ قبل برگرداند.

**تغییر (`chart-ohlc-extractor.js`):** کلید «تأیید» دیگر هیچ‌وقت غیرفعال نمی‌شود — نه در
شروع، نه با آمدن تصویر تازه، نه بسته به نتیجهٔ استخراج. رفتار دوشاخه است:
- اندازه‌گیری هست → مثل قبل: تحویل به موتور + چارت روی «محیط الگو» + بستن پنجره.
- هیچ‌چیز نیست → فقط می‌بندد؛ هیچ رکوردی نوشته نمی‌شود، overlay و کتابخانه دست نمی‌خورند.

**نسخه‌ها:** `?v=35`، `sw.js` → `chartdna-v2.33.0-confirm-closes-empty`.

**تست‌ها:** smoke ‏42/42 (کلید فعال، بستن پنجرهٔ خالی، تحویل‌ندادن هیچ‌چیز)، ‏ui-trim ‏62/62،
‏ui.test.js به‌روز (۱۳۲ سنجه — دو سنجهٔ disabled قدیمی بازنویسی شد).

**برگشت:** `git revert <این کامیت>`؛ v32 = `git revert 17fbe3e`.

## v34 — پاک‌سازی کامل جای پای موتور ۲ (وصله‌های P12–P20)

تا v33، موتور ۲ «خنثی» بود ولی گره‌های مخفی‌اش هنوز در DOM ساخته می‌شدند و متن‌هایش در
باندل مانده بود. حالا هیچ‌کدام اصلاً رندر نمی‌شوند:

- **P12+P13:** دو کلید داک «تراز مقیاس قیمت» و «استخراج الگو از کادر» → `!1&&` (هیچ گرهی ساخته نمی‌شود).
- **P14:** کلید تب «تشخیص و ثبت الگو از تصویر» در تنظیمات → `!1&&`.
- **P15:** کارت «تعداد نقاط شباهت و تفکیک الگو» → `!1&&`.
- **P16+P17:** هر دو نشان «pts» (روی «محیط الگو» و «اطلاعات الگوی کشف شده») → `!1&&`.
- **P18+P19:** هر دو چیپ «قیمت مرجع» (سر کارت برش و سربرگ مودال نمودار کندلی) → `!1&&`.
- **P20:** ۳۰ رشتهٔ ترجمهٔ مردهٔ AI Vision (aiVision/aiVisionTitle/aiVisionDesc در ۱۰ زبان) خالی شد؛
  «بینایی هوش مصنوعی» دیگر هیچ‌جای باندل نیست.

وصله‌های قبلی (P1–P11) سر جایشان‌اند؛ `chart-dna-ui-trim.js` هم به‌عنوان کمربندِ دوم برای
باندل‌های کش‌شدهٔ قدیمی دست‌نخورده ماند (روی باندل تازه دیگر چیزی برای مخفی‌کردن پیدا نمی‌کند).

**نسخه‌ها:** `?v=35`، `sw.js` → `chartdna-v2.34.0-engine2-erased`.

**تست‌ها:** smoke ‏48/48 (۶ سنجهٔ جدید P12–P20 + همهٔ سنجه‌های قدیمی P1–P11 برقرار)، ‏ui-trim ‏62/62.

**برگشت:** `git revert <این کامیت>`؛ v33 = `git revert c43db93`.

## v35 — تصاویر آپلودشده دیگر ذخیره نمی‌شوند؛ بخش Image در «مدیریت داده‌ها» حذف شد

**خواسته:** موتور ۱ هرگز نباید در میان تصاویر آپلودشده جستجو کند؛ اطلاعات تصویر فقط موقت
(به‌عنوان سؤالِ جستجو) کاربرد دارد و نباید ذخیره شود.

**تغییرها (`chart-ohlc-extractor.js` → نسخهٔ ۲۰):**
1. **«تأیید» دیگر هیچ دیتاستی نمی‌نویسد.** رکورد `pxrec-*` (نماد IMAGE) که قبلاً به IndexedDB
   می‌رفت و در «مدیریت داده‌ها» بخش Image را می‌ساخت حذف شد؛ `selectDataset` هم (که آن را به
   فهرست جستجو اضافه می‌کرد) پاک شد. خروجی تأیید: `dataset: 'not-stored'`.
2. **الگوی استخراجی فقط یک «پرسش موقت» است:** تک‌ورودی `custom_dna_px_*` در کتابخانهٔ الگو
   که با هر تأیید بعدی **جایگزین** می‌شود (`dropPxPatterns` — کتابخانه هیچ‌وقت انباشته نمی‌شود)؛
   متن یادداشتش هم می‌گوید موقت است و دیتاستی ذخیره نشده.
3. **جاروی یک‌باره (`dropImageDatasets`، پرچم `chartdna_px_datasets_swept`):** همهٔ دیتاست‌های
   تصویری موجود (`pxrec-*` یا `symbol === 'IMAGE'`) از IndexedDB حذف و شناسه‌هایشان از
   `chartdna_selected_dataset_ids` پاک می‌شوند — دیتاست‌های خود کاربر دست نمی‌خورند.

**نسخه‌ها:** `?v=35`، `sw.js` → `chartdna-v2.35.0-no-image-datasets`.

**تست‌ها:** smoke ‏52/52 (بخش جدید 3.5: جاروی دو دیتاست تصویری از استور ساختگی، پاک‌سازی
انتخاب، مصون ماندن دیتاست کاربر، نبودِ put در کد)، ‏ui-trim ‏62/62، ‏ui.test.js بازنویسی بخش ۴
(۱۲۸ سنجه).

**برگشت:** `git revert <این کامیت>`؛ v34 = `git revert 6085298`.

## v36 — search methods as checkboxes (chart-dna-methods.js)

The four weight sliders («وزن‌دهی فرمول» tab) are gone; Engine-1 search methods are now
user-ticked checkboxes injected into the «پارامترهای جستجو» pane (card `#dna-methods-card`).

- Bundle patches: **P21** routes all 3 matcher call sites (`analyzeMultiDatasets` ×1,
  `findMatchesInCustomPatterns` ×2) through `window.__dnaHook(yn)`, so the hook wraps the
  statics before the first search; **P22** unrenders the weights tab button and pane (`!1&&`).
- New script `chart-dna-methods.js` (?v=36, defer, precached). Ticks persist in
  localStorage `chartdna_search_methods` (JSON). Defaults: pearson/dtw/slope/extrema ON;
  ohlc4/anatomy/multiscale/fastscan OFF.
- Methods 1–4 map to `config.weights` as equal shares of the ticked set (pearson-only
  fallback when none of the four is ticked).
- Method 5 (four-channel OHLC) and 6 (candle anatomy) re-score each hit against the
  confirmed image query in localStorage `chartdna_px_overlay` (50/50 blend into
  `similarity`, threshold re-applied); silently skipped when no image query exists.
- Method 7 (multi-scale) runs the original matcher at patternLength ×0.75 / ×1 / ×1.3 and
  dedupes overlapping ranges (>50 % overlap keeps the best).
- Method 8 (fast screen) on datasets ≥3000 candles: coarse Pearson pass on every-4th
  close, candidate regions expanded/merged, original matcher run on slices, indices
  offset back.
- Kill switch: `localStorage.chartdna_methods='0'` → hook returns the matcher untouched
  (original config weights, no card). Seam: `window.ChartDnaMethods` (v36).
- Revert: `git revert <v36 commit>`.

## v37 — green play searches the Engine-3 image pattern (px-play)

The play button (`#btn-start-analysis`) used to depend on the removed Engine-2 state
(`W`); now it runs Engine 1 on the pattern confirmed from the uploaded image.

- **P23**: the play handler falls back to `window.__pxQuery()` (closes of the confirmed
  `chartdna_px_overlay` record) when the app has no extracted pattern, adopts it into app
  state (`ee(q)` + pattern title), then runs the ORIGINAL `Je` pipeline — custom-library +
  TA-Lib + `analyzeMultiDatasets`, all through the v36 `__dnaHook` (ticked methods),
  results rendered in «اطلاعات الگوی کشف شده» and the results list as before.
- **P24**: `hasPattern` is also true when `window.__pxQuery()` returns a pattern, so
  re-renders keep the button enabled natively.
- Script side (`chart-dna-methods.js`, seam v37): `window.__pxQuery` (cached ~0.8 s),
  `armPlay()` un-disables the button between React renders (MutationObserver on
  `disabled` + 1.5 s interval); never touches the button while an analysis is running
  (`#btn-stop-analysis` enabled) and never enables it without a confirmed pattern.
- Kill switch: `localStorage.chartdna_pxplay='0'` → `__pxQuery` returns null, P23/P24
  fall back to original behaviour, button untouched.
- Revert: `git revert <v37 commit>`.

## v44 — a numbered six-key row above the «ورود تصویر» window's own key row

Request: «یک ردیف ۶ کلیدی در بالای پنجرهٔ ورود تصویر، دقیقاً هم‌سایز ردیفِ
شش‌کلیدیِ فعلی، فعلاً با اعداد نام‌گذاری شود تا بعداً به هر کلید وظیفه‌ای داده شود.»

- The row lives at the top of «ورود تصویر» (`#ohlc-card`), directly above the window's
  own six-key row (`#ohlc-bar`), in `chart-ohlc-extractor.js`.
- Geometry is a copy of the existing row: same classes (`ohlc-bar … p-1.5 … gap-1`),
  36 px keys, 4 px gap, 6 px padding → the same 360×50 box (keys 54×36 at desktop
  sidebar width, measured live). Distinct `#ohlc-fn-bar` id + subtle darker background
  (`#101b2d`) so the placeholder band reads as a separate strip.
- Keys: `#ohlc-fn-1` … `#ohlc-fn-6`, `type=button`, face = the Persian digit ۱…۶
  (`span.fn-num`), title/aria = «کلید ۱» … «کلید ۶». Inert on purpose: no click handler,
  no state, no effect — wiring only. Assignments later only need
  `document.getElementById('ohlc-fn-N').addEventListener('click', …)`.
- Original row untouched (`#ohlc-bar` now also carries `row-0` purely as a CSS hook).
- Cache: `index.html`/`sw.js` bumped to `?v=44` / `chartdna-v2.44.0-fn-keys-row`.
- Revert: drop the `v44` blocks from `chart-ohlc-extractor.js` (STYLE + `faDigit`/
  `fnKeyHtml` helpers + the `#ohlc-fn-bar` markup + the `row-0` class) and restore
  `?v=43` / the sw version.

## v45 — live TradingView price in «ورود تصویر», on key ۱ (tv-live)

Request: نمایش زندهٔ قیمت تریدینگ‌ویو در پنجرهٔ «ورود تصویر» با انتخاب نماد/تایم‌فریم و
کلیهٔ قابلیت‌های خود TV؛ کلید ۱ از ردیف شماره‌دار (v44) به این قابلیت اختصاص داده شد.

- Section `#ohlc-tv` sits in the window right under the two key rows, above the
  picture stage. Toggled by `#ohlc-fn-1` (title «قیمت زنده — کلید ۱»), persistent in
  localStorage `chartdna_tv_open` (`'0'` = closed at boot; default open).
- Chart: TradingView **Advanced Chart** widget (`s3.tradingview.com/tv.js`, no API
  key) with the full TradingView toolbox — symbol search, timeframes, indicators,
  drawing tools, save-image. No extra quick chips (by request). Defaults:
  `OANDA:XAUUSD`, interval 60 (H1), dark theme, timezone Asia/Tehran, locale en
  (fa not offered by TV; the rest of the page stays Persian). Widget created lazily
  on first open; script error → warning in `#ohlc-status`, never blocks the panel.
- Two sizes (request «both_sizes»): inline in the window (stage 300px) and a
  fullscreen mode — `#ohlc-tv-expand` adds `.full` to the *same* section
  (`position:fixed` CSS, no iframe moves), Esc or the button returns to the frame.
- Key ۱ shows an `.on` ring while the live price is open.
- The capturable own-data view (screenshot → OHLC engine, Binance source) is
  **deferred** by request; the numbered keys ۲–۶ are still unassigned.
- Cache: `index.html`/`sw.js` → `?v=45` / `chartdna-v2.45.0-tv-live-price`.
- Revert: drop the v45 CSS/HTML/JS blocks from `chart-ohlc-extractor.js` and
  restore `?v=44` / the sw version.

### v46 — the TradingView picture zoomed out («watching TV»)

Request: «تصویر تریدینگ‌ویو داخل پنجره را زوم‌اوت کن که کوچک‌تر شود، انگار کاربر در
این پنجره تلویزیون نگاه می‌کند.»

- The widget is created with `range:'12M'` (long visible range → candles read small).
  Override any time: `localStorage.chartdna_tv_range` (e.g. '6M', 'ALL').
- Whole picture scale: the TV frame now draws at `scale(0.78)` inside the dark stage —
  the picture is smaller with a dark bezel around it, exactly like a TV seen from the
  sofa. Persisted `chartdna_tv_scale` (0.5–1). New head keys: **−** (دورتر) and **+**
  (نزدیک‌تر). Fullscreen (`#ohlc-tv.full`) always lifts the transform to full size and
  hides the ± keys; leaving fullscreen restores the saved scale.
- Verified live: transform applied at boot, −/＋ change 0.68/0.78, fullscreen computed
  `transform:none`, right-edge candle pixels 960 → 87 (content moved inward).
- Revert: drop the v46 scale blocks/keys from `chart-ohlc-extractor.js`, restore
  `?v=45` and the sw version.

### v47 — display models for the live TradingView view, cycled by کلید ۲

Request: چند مدلِ نمایشِ سبک‌تر برای چارت زنده (نمای فعلی شلوغ است) + چرخش با دکمهٔ ۲.

- Three display models (widget style codes verified live: 1 = candles, 4 = thin line):
  1. **«کندل ساده»** (default) — clean candles, top/side toolbars hidden (watch-only)
  2. **«خط سبک»** — thin line chart, toolbars hidden
  3. **«نمای کامل TV»** — full TradingView toolbox (symbol search, timeframes, indicators…)
- کلید ۲ (`#ohlc-fn-2`) cycles candle-lite → line-lite → full → …; each press rebuilds the
  widget iframe for the chosen model (scale/TV-distance and fullscreen still apply).
- Current model is shown in a pill in the live-view header and in the کلید ۲ tooltip;
  a one-line status confirms each switch («سبک نمایش چارت زنده: … · 2 از 3»).
- Persisted `localStorage.chartdna_tv_mode` (id). Cycle order is the array order, so a
  stored index always resolves. Default = the lightest model.
- Cache: `?v=47` / `chartdna-v2.47.0-tv-modes-cycle`.
- Revert: drop the v47 mode blocks from `chart-ohlc-extractor.js`, restore `?v=46`.

### v48 — duty icons on کلیدهای ۱ و ۲ (same lucide set as the row below)

Request: «به کلید شماره ۱ و ۲ آیکونی که نشان‌دهنده وظیفه‌شان است اختصاص بده؛ شکل آیکون‌ها
با کتابخانهٔ آیکون‌های ردیف پایینی همخوانی داشته باشد و همه از یک کتابخانه باشند.»

- Key ۱ (live TradingView price) face is now the lucide **tv** glyph; key ۲ (display
  model cycle) the lucide **palette** glyph. Keys ۳–۶ keep their Persian digits.
- The glyph node data was extracted from the exact same icon library the window's own
  row was copied from (`lucide-react` **v0.475.0**, verified against the installed
  package) and rendered by the same `LUCIDE()` helper → identical
  `class="lucide lucide-… w-4 h-4 group-hover:scale-110…"` svgs at 16×16 px, so both
  rows read as one family. Tooltips/aria still carry the key numbers and duty.
- Cache: `?v=48` / `chartdna-v2.48.0-fn-duty-icons`.
- Revert: drop the ICO `tv`/`palette` entries, pass digits again in the fn row markup,
  restore `?v=47`.

### v50 — default timeframe of the live TradingView window is 1 hour

Request: «پنجره تریدینگ وید الان به صورت پیش فرض بر روی تایم فرم یک روزه است؛ پیش فرض
تایم فرم رو بر روی ۱ ساعته بذار.»

- Root cause: the widget was already built with `interval:'60'`, but TradingView
  re-picks the interval to fit the requested `range` — probed live on fresh
  browsers: `range '12M' → 1 day`, `'6M' → 2 hours`, `'3M' → 1 hour`, `'1M' → 30m`,
  no range → 1 hour. The v46 default `range:'12M'` was what forced the daily chart.
- Fix: default `range` is now `'3M'` (the widest preset that keeps the 1-hour
  interval) while `interval:'60'` is unchanged — verified in the widget's own
  full-model toolbar: the active timeframe radio is «1 hour».
- The zoomed-out look is preserved: 3 months of hourly candles still fill the
  pane with small candles. A manual override stays possible as before via
  `localStorage.chartdna_tv_range` (and, being the user's own choice, may switch
  the timeframe again — documented in the code comment).
- Cache: `?v=50` / `chartdna-v2.50.0-tv-default-1h` (v49 numbering was consumed
  by the reverted camera-snapshot build).
- Revert: set the default `range` back to `'12M'` and restore `?v=48` / the sw
  version `chartdna-v2.48.0-fn-duty-icons`.

### v51 — کلیدهای ۴–۶: پلی و جستجو · استاپ · سطل آشغال (انتقال از نوار زیر «محیط الگو»)

Request: کلیدهای سطل آشغال و استاپ و پلی که زیر پنجرهٔ «محیط الگو» هستند با همان
کارکرد و اتصالات به کلیدهای شماره‌دار بالای پنجرهٔ «ورود تصویر» منتقل شوند —
کلید ۶ = سطل آشغال، ۵ = استاپ، ۴ = پلی و جستجو — و از آن محیط هم حذف شوند.

- The app's control strip below the «محیط الگو» window is `#remote-control-deck`
  (four keys: import-image — duty already moved in v45 — plus `#btn-start-analysis`
  «شروع تحلیل DNA الگو» / `#btn-stop-analysis` «توقف تحلیل» / `#btn-clear-all`
  «پاکسازی محیط و داده‌ها»). The whole strip is now hidden by CSS
  (`#remote-control-deck{display:none!important}`) — all its duties live in the row.
- کلیدهای ۴/۵/۶ of the numbered row get the duty icons play / square / trash-2
  (glyphs copied from the same lucide-react v0.475.0 set — the very glyphs the
  deck buttons render: `lucide-play`, `lucide-square`, `lucide-trash-2`), same
  16×16 `w-4 h-4` face as the rest of the row.
- Same behaviour and connections, not a copy: the original buttons stay alive in
  the DOM (invisible) and each fn key forwards a REAL click to its original
  (`btn-start-analysis`/`btn-stop-analysis`/`btn-clear-all`) — React's own handler
  runs, chart-dna-methods keeps arming the play key, and our capture hook on
  `#btn-clear-all` (fullReset) fires exactly as before. Clicking while the original
  is disabled is ignored (same rule as the original key).
- The enabled/disabled state of each original is mirrored onto the fn key via
  `aria-disabled` (dimmed look + «فعلاً غیرفعال» in the tooltip), polled briefly
  after mount.
- Cache: `?v=51` / `chartdna-v2.51.0-fn456-deck-duties`.
- Revert: remove the v51 block + ICO play/square/trash2, drop the icons from the
  fn row markup, remove the `#remote-control-deck` hide rule, restore `?v=50`.

### v52 — play/stop/trash semantics: engine-1 search from engine-3 data + shape fallback + full trash reset

Request: کلید پلی باید موتور ۱ را با دادهٔ به‌دست‌آمده از موتور ۳ روی دیتاست‌های
انتخاب‌شده به جستجوی الگوی مشابه ببرد؛ استاپ همهٔ موتورها را متوقف کند؛ سطل آشغال همهٔ
تصاویر آپلودشده را پاک کند و برنامه مثل باز شدن تازه شود. Answer: اگر قیمت‌ها (O/H/L/C)
خوانده شوند جستجو با همان‌هاست؛ اگر خوانده نشوند، جستجوی شکلی (روش پیشنهادی).

- Key ۴/۵/۶ forward real clicks to the app's own `btn-start-analysis` /
  `btn-stop-analysis` / `btn-clear-all` (v51). This v52 makes the chain work when the
  price axis is NOT readable (the case for every TradingView-widget shot probed, at
  any size/DPI): previously confirm then handed nothing over and play stayed dead.
- Engine-3 hand-off fallback: when calibration is missing, each measured candle's
  pixel rows map 1:1 to a *relative* price (price falls as the pixel row grows, exactly
  like a real axis), so the closes keep the true shape and direction of the candles —
  engine-1 normalizes the query anyway and searches by shape. Nothing absolute is
  invented: the pattern is named «Pixel shape (نسبی)» and both the notes and the
  status line say the search is shape-only. When the axis IS read, the real
  open/high/low/close values are handed over as before («Pixel reconstruction»).
- The overlay record (`chartdna_px_overlay`) is written in both cases → the app's own
  play key (`btn-start-analysis`) arms (fn ۴ un-disables, verified live after confirm:
  ovStore=true, pxRecs=1, fn4 aria-disabled=false) and engine-1 can start with the
  engine-3 pattern over the user-selected datasets; stop = the app's cancel flag.
- Trash (fn ۶) now also resets the «ورود تصویر» window itself to its fresh-launch
  look: loaded image cleared, all three canvases emptied, views/table hidden, status
  «هنوز تصویری انتخاب نشده.», the file input reset, and the window's own px pattern
  records (stored + pending) dropped — on top of the existing overlay/env cleanup.
- Verified live (injected v52): after confirm with an axis-less image the play key is
  armed and one px pattern + overlay record exist; after fn ۶ the window is back to
  boot state (img null, ovStore false, pxRecs 0, table/views hidden) with no errors.
- Cache: `?v=52` / `chartdna-v2.52.0-engine1-handoff-trash`.
- Revert: remove the v52 barPrice fallback + resetInputPanel changes, restore `?v=51`.

### v53 — play actually runs engine 1 (direct handler invocation) — v52 follow-up fix

Report: «کلید پلی اجرا نشد. نوار پیشرفت اسکن هم هیچ حرکتی نمیکند. در قبل این کلید
وظیفهاش انجام میشد… وظایف را به خوبی جایگزین نکردی.»

- Root cause (bisected live on the deployed page): with this companion script on the
  page, React no longer dispatches (delegated) click events to the deck's buttons —
  the click bubbles to the React root but the bound `onClick` never runs (real mouse
  click AND programmatic `.click()` both dead; the settings/header buttons still
  work; with the extractor blocked entirely the same play click runs fine). The deck
  button still carries its live `__reactProps$…onClick` (React's own `xt` handler) —
  calling that function directly runs the WHOLE engine-1 flow: verified live, the
  analysis hook fires 3× and «پیشرفت اسکن» reaches 100 with results rendered.
- Fix: fn keys ۴/۵/۶ now invoke the very React handler bound to the original button
  directly (`__reactProps$…onClick`, found fresh at click time, with `.click()` as a
  fallback) instead of forwarding a click event. Same code path and gating as the
  original key — nothing reimplemented. fn ۶ additionally runs our own `fullReset()`
  (its document click-hook no longer fires since no real click is dispatched).
- Verified live (served the v53 file over the deployed page): after confirm, fn ۴
  click → stop enabled, progress s:12→100, «شباهت» results rendered, hook 3; fn ۵
  mid-run → analyzing stops (stop disabled, start re-enabled); fn ۶ after loading a
  second image → window back to fresh (image null, overlay false, «هنوز تصویری
  انتخاب نشده.», progress 0). No page errors.
- Cache: `?v=53` / `chartdna-v2.53.0-play-direct-handler`.
- Revert: restore the click-forwarding in `wireDeckDuties` and `?v=52`.

### v54/v55 — play arms instantly after extraction; clear disabled-state feedback

Follow-up to v53: play still reported dead («هیچ کدام انجام نشده»).

- v54: a successful extraction now hands the measured candles to engine-1's query
  spot IMMEDIATELY (no «تأیید» step needed) — writeExtracted + overlay record run
  at the end of `run()` when bars are ok, so fn ۴ un-disables right away, exactly
  like the app's own extract→pattern→search flow. «تأیید» afterwards no longer
  double-hands (autoHanded flag). Clicking fn ۴ while disabled shows an explanatory
  status instead of silence.
- v55: instant arming without poll delay — `pxPlayArmNow()` reads the overlay record
  directly, enables the app's own play button the way armPlay does, and the fn-۴
  mirror (syncOne) never lags behind. Fixes the ~1-2 s race seen on the live site
  where the key stayed dim right after extraction.
- Live-verified on the deployed v55: 600 ms after «استخراج کندل‌ها» (no confirm)
  fn ۴ is armed; clicking it runs engine-1 — scan progress s:0→100, «شباهت» results
  rendered, analysis hook fires; stop/trash behave as in v53.
- Cache: `?v=55` / `chartdna-v2.55.0-play-arm-instant` (sw.js keeps skipWaiting +
  clients.claim + network-first navigations, so an old cached client updates on its
  next ordinary reload).
- Revert: drop the auto-arm block in run() + pxPlayArmNow and restore `?v=53`.
