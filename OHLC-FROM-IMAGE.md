# استخراج OHLC از تصویر نمودار (Image → Candles)

سه اسکریپت در ریشهٔ مخزن، بدون بیلد و بدون وابستگی شبکه:

| فایل | نقش |
|---|---|
| `chart-ohlc-engine.js` | موتور بینایی (`window.ChartDNACV`) — کالیبراسیون محور قیمت، یافتن شبکهٔ کندل‌ها، اندازه‌گیری بدنه/شدو، اطمینان، CSV، دو رندر روی **قابِ خودِ تصویر** (`renderAnnotated` برای نمای مارک‌دار، `renderReconstructed` برای کندل‌های بازسازی‌شده) و یک رندر مستقلِ خودتنظیم (`renderChart`) |
| `chart-ohlc-extractor.js` | پنجرهٔ «ورود تصویر»: تصویر می‌گیرد، کندل‌ها را از پیکسل اندازه می‌گیرد و CSV و تصویر حاشیه‌نویسی‌شده می‌دهد. دکمهٔ اختصاصی ندارد — کلید «ورود تصویر چارت» نوار پخش ( `#btn-import-image` ) همین پنجره را باز می‌کند. هیچ‌چیز در دادهٔ برنامه نمی‌نویسد |
| `chart-dna-ui-trim.js` | برداشتن پنجره‌هایی که قرار بود نباشند (از جمله بلوک آپلود وسط «محیط الگو») + جاروکردن دادهٔ باقی‌مانده (پایین همین سند) |

`index.html` هر سه را با `?v=21` بارگذاری می‌کند و `sw.js` آن‌ها را در `PRECACHE_URLS` دارد
(نسخهٔ کش: `chartdna-v2.21.0-deck-style-key-row`). برای انتشار مجدد فقط `?v=` را بالا ببرید.

## چه کاری انجام می‌دهد (و چه کاری نمی‌کند)

فقط پیکسل‌ها خوانده می‌شود؛ هیچ دادهٔ قیمتی از اینترنت یا «حافظهٔ مدل» نمی‌آید:

1. ماسک رنگی کندل‌های صعودی/نزولی؛ جداکردن اجزای رابط کاربری (تیکر هدر، برچسب قیمت،
   پنل حجم، آیکون‌ها) بر پایهٔ ریخت‌شناسی — نه مستطیل از پیش تعریف‌شده.
2. بازیابی شبکهٔ کندل: جست‌وجوی فاز/گام روی مرکزِ بدنه‌ها + رگرسیون کمترین مربعات،
   رتبه‌بندی با «پوشش یکنواخت روی کل پهنای پلات».
3. کالیبراسیون محور عمودی: خطوط راهنما از اختلاف روشنایی پس‌زمینه (با فیلتر ضد دیتر)،
   اعداد محور با OCR رقمی بدون وابستگی خارجی (قالب ۱۴×۲۰، همبستگی نرمال‌شده)،
   مقایسهٔ مدل خطی و لگاریتمی، و آزمون مستقل با برچسب قیمتِ روی محور.
4. برای هر کندل: بدنه از ردیف‌هایی که تقریباً کل پنجرهٔ ±۲ پیکسلی را پر کرده‌اند،
   High/Low از انتهاهای جزء به هم پیوستهٔ همان کندل (حلقه‌های متصل)، ترمیم ردیف‌های
   پوشیده‌شده توسط خط‌چین قیمت آخر.
5. اطمینان هر کندل از قوانین داخلى (O/H/L/C)، پیوستگی close→open با همسایه، ضخامت بدنه،
   مرز پنل و ابهام رنگ. هر چه اندازه‌گیری نشود **خالی** می‌ماند، نه حدس.

## اتصال به برنامه: حذف شد

این پنجره از نسخهٔ `chartdna-v2.17.0` **فقط‌خواندنی** است. چهار کنترل آن و کل مسیر
نوشتنشان برداشته شد: «📷 انتخاب تصویر از حافظهٔ دستگاه»، «استفاده از تصویری که در
برنامه آپلود کرده‌اید»، «ثبت به‌عنوان دیتاست Chart DNA» و «ثبت و اجرای جستجو» — با
عملکرد و همهٔ اتصالاتشان:

- `saveDataset(...)` و `openDb()` و نوشتن در `ChartDNA_Storage` / `market_datasets`؛
- `appendPattern`، نرمال‌سازی سری، `norm`، و صفِ `chartdna_ohlc_pending_pattern` در
  sessionStorage (که برای الحاق بی‌خطر به کتابخانهٔ الگوهای برنامه لازم بود)؛
- افزودن شناسه به `chartdna_selected_dataset_ids` و گزینه‌های «الگو اضافه شود» /
  «فقط این دیتاست انتخاب شود» (کل جعبهٔ «اتصال به موتور Chart DNA» از پنل برداشته شد،
  چون فقط همین دو تیک بود)؛
- پرچم `chartdna_ohlc_autosearch`، تابع `reloadPage()`، ریلود خودکار صفحه و کلیک
  خودکار دکمهٔ «جستجو»ی برنامه (بلوک `autoSearch`)؛
- درِ `window.ChartDnaOhlc.saveDataset` و `window.ChartDnaOhlc.reload` هم بسته شد؛
  `useImage`، `run`، `toCSV`، `report`، `open`، `close`، `confirm`، `confirmed`، `deckKey` و
  `deckTakeover` سر جایشان‌اند؛
- `chart-dna-ui-trim.js` اگر نسخهٔ قدیمی در کش کسی هنوز این دکمه‌ها را بسازد، آن‌ها را
  از DOM برمی‌دارد (به `STALE` اضافه شدند، با شمارندهٔ کرانه‌دار).

**دیگر هیچ‌چیز نوشته نمی‌شود.** آخرین چیزی که ما در localStorage می‌گذاشتیم
`chartdna_ohlc_form` بود — همان یادداشت‌های نماد/تایم‌فریم/تاریخِ پنل «برچسب دیتاست» —
و با خودِ پنل حذف شد (بخش «سه پنل کناری» پایین همین صفحه): اطلاعات برچسب فقط برای همان
لحظهٔ کاربر است، در حافظهٔ کوتاه‌مدتِ همان مرحله نگه داشته می‌شود و بعد پاک.
جاروی دادهٔ قدیمیِ ساختهٔ خودِ افزونه (`market_datasets`، `chartdna_saved_patterns`،
`chartdna_ohlc_*`، `chartdna_reference_price`) هم مثل قبل در `chart-dna-ui-trim.js` است و
یک‌بار اجرا می‌شود، پس `chartdna_ohlc_form`ِ نسخهٔ قدیمی هم از حافظه بیرون می‌رود.

**برگرداندن:** `git checkout 7dbf47b -- chart-ohlc-extractor.js` (همراه با بالا بردن
`?v=` در `index.html` و `sw.js`) — همان آخرین نسخه‌ای که دیتاست و الگو می‌نوشت.

### تاریخ و ساعت

تاریخ/ساعت **همیشه خالی** است. پیش‌تر لنگرها را کاربر داخل پنل «برچسب دیتاست» می‌داد
(دو تاریخ، یا تاریخ شروع + ساعت شروع + تایم‌فریم)؛ آن پنل حذف شده، پس منبعی برای
`Date`/`Time` نمانده و ما هم تاریخی نمی‌سازیم. دو ستون در CSV هستند ولی خالی می‌مانند
(ساختار فایل عوض نشده). `assignDates` و پارامتر `extraRefs` تنها در موتور
(`chart-ohlc-engine.js`) و تست برابری (`parity.test.js`) زنده‌اند تا خودِ مسیر
اندازه‌گیری دست‌نخورده بماند؛ هیچ راهی از پنجره به آن‌ها نمی‌رسد.

## اعتبارسنجی

موتور روی همان اسکرین‌شات XAUUSD 1h که با پایتون/NumPy استخراج و بازبینی شده بود اجرا شد
(تست‌های `dev-tests` در محیط توسعه): ۲۹۶ کندل، **۹۹.۶٪** از ۱۱۸۴ سطح OHLC اختلافشان با
خروجی پایتون کمتر از یک پیکسل (≤۰.۶۱ دلار) بود، میانهٔ اختلاف ۰.۲۲ دلار، صفر اختلاف در
جهت کندل، و تاریخ‌ها ۲۹/۲۹۶ یکسان. خطای آزمون مستقلِ برچسب قیمت ۰.۰۱۶ دلار.

## کلید «ورود تصویر چارت» حالا محیط بازسازی OHLC را باز می‌کند

کلیک روی آن کلید (آیکون تصویرِ تنها، `#btn-import-image` داخل ردیف `#remote-control-deck`
زیر `#sidebar-controls`، با عنوان «ورود تصویر چارت») دیگر کاربر را به حافظهٔ دستگاه برای
انتخاب فایل نمی‌برد؛ **محیط بازسازی OHLC از اسکرین‌شات چارت** را باز می‌کند و انتخاب عکس از
حافظه همان‌جا انجام می‌شود. ظاهر کلید، آیکون، نام و markup‌اش اصلاً عوض نشده — فقط مقصدش.

- **چطور جلوی پنجرهٔ حافظه گرفته می‌شود:** در باندل، خودِ handlerِ کلیک یک
  `<input type="file">` می‌سازد و `click()` صدا می‌کند (گرهی که هرگز به DOM اضافه نمی‌شود:
  `onSelectImageClick:()=>{const f=document.createElement("input");… f.click()}`)، پس هیچ
  چیزی برای unbind کردن وجود ندارد. تنها راهِ بسته‌ماندن پنجرهٔ حافظه این است که آن
  handler کلاً اجرا نشود: شنوندهٔ **capture** روی `document` کلیک را می‌خورد
  (`preventDefault` + `stopPropagation` + `stopImmediatePropagation`) پیش از آنکه شنوندهٔ
  delegated خودِ React روی ریشهٔ برنامه آن را ببیند.
- **داخل پنل:** ردیف شش‌کلیدی بالای پنجره (فرمت ردیفِ زیر «محیط الگو» در صفحهٔ اول —
  توضیح کامل در بخش «ردیف شش‌کلیدیِ هم‌فرمتِ نوار پخش»): کلید اول تصویر را از حافظه می‌گیرد
  (یک لمس = `input[type=file]` خودِ پنل؛ رها‌کردن فایل روی همان کلید و Ctrl+V هم کار
  می‌کند)، کلید دوم اندازه‌گیری را اجرا می‌کند، سه کلید بعدی نما را عوض می‌کنند و ششمی
  «تأیید» است. خروجی‌ها همین‌جا می‌مانند، زیر همان قاب: جدول کندل‌ها، یک خط وضعیت کوتاه
  (بارگذاری تصویر، تعداد کندل‌ها، خطا و هشدار) و دکمه‌های «دانلود CSV» و «دانلود تصویر
  حاشیه‌نویسی‌شده». هیچ پنل کناری و هیچ جعبهٔ تنظیمی اضافه نیست.
- **هیچ جای دیگری از برنامه دیگر خودکار اندازه‌گیری نمی‌شود.** شنوندهٔ `FileReader` که در
  نسخهٔ قبل همهٔ انتخاب‌های تصویر را می‌خواند حذف شده است؛ دکمهٔ «انتخاب تصویر» کارتِ برش و
  «آپلود تصویر چارت»ِ سربرگ DNA Vision دقیقاً کار قبلی خودشان را می‌کنند و پنل ما را باز
  نمی‌کنند. تصویرِ اندازه‌گیری‌شده فقط همان است که کاربر در پنل ما انتخاب می‌کند.
- اگر کلیدِ نوار پخش در صفحه نبود (نسخهٔ دیگری از برنامه)، هیچ‌چیز تصاحب نمی‌شود و
  برنامه دست‌نخورده است؛ `window.ChartDnaOhlc.deckKey()` همان کلید را برمی‌گرداند یا
  `null`، و `deckTakeover()` می‌گوید تصاحب فعال است یا نه.
- **خاموش/روشن:** `localStorage.chartdna_deck_takeover = '0'` → آن کلید دوباره فقط کار
  خودش را می‌کند (انتخاب فایل از حافظه) و پنل باز نمی‌شود؛ با پاک‌کردنش تصاحب برمی‌گردد.
- نام پنجره‌ای که باز می‌شود **«ورود تصویر»** است و هیچ توضیحی زیرش نوشته نمی‌شود
  (توضیح قدیمیِ «computer vision…» و برچسب مردهٔ `T.open` از فایل UI برداشته شدند؛
  `margin` زیر عنوان هم به صفر رفت تا فاصلهٔ خالی جایش نماند).
- پنل را می‌توان با `window.ChartDnaOhlc.open()` هم باز کرد؛ بستن با کلید ششمِ ردیف
  («تأیید»)، کلیک روی پس‌زمینه، `Esc` یا `window.ChartDnaOhlc.close()` است. اگر جایی تصویر را به‌صورت برنامه‌ای بدهید، `onPickedImage(dataURL)` همان را
  اندازه می‌گیرد (تست‌ها از همین درِ وارد استفاده می‌کنند).
- اگر نسخهٔ قدیمی در کش کسی هنوز `#ohlc-tool` یا `#ohlc-auto-card` را ساخته باشد،
  `chart-dna-ui-trim.js` آن‌ها را از DOM برمی‌دارد (فهرست `STALE`).

## چه چیزی حذف شد (و چطور برگردانده می‌شود)

این پنجره‌ها مرحله‌به‌مرحله از برنامه برداشته شده‌اند (۱ تا ۳ در
`chartdna-v2.11.0-remove-pattern-panels`، ۴ در `chartdna-v2.12.0`، ۵ در
`chartdna-v2.15.0-crop-upload-removed`):

1. **کارت «کندل‌های بازسازی‌شده از همین تصویر (OHLC از پیکسل)»** — کل
   `chart-ohlc-autopilot.js` حذف شد، یعنی: استخراج خودکار از تصویری که کاربر در
   «محیط الگو» آپلود می‌کند، رسم کندل‌ها در محیط برنامه، ثبت خودکار دیتاست، افزودن
   خودکار الگو به کتابخانه، هم‌گام‌سازی «قیمت مرجع بازار»، دنبال‌کردن کادر زردِ برش و
   بازخوانی/کلیک خودکار «جستجوی فوری» DNA.
2. **پنل «رسم چارت و سطوح تحلیل الگو» (Pattern Chart Overlay) خودِ برنامه** — بومِ
   همپوشانی، خطوط حمایت/مقاومت، تارگت‌ها و برچسب قیمت.
3. **«کارت‌های آماری تحلیل الگو» (Pattern Stats & Targets)** — کارت‌های آماری زیر آن
   (سقف/کف‌ها، سطوح ورود/تارگت/استاپ و متن «آماده»).

   هر دو در باندل مینیفای‌شدهٔ `assets/index-DOCOgEgh.js` هستند و سورسی در این مخزن
   ندارند، پس `chart-dna-ui-trim.js` آن‌ها را **مخفی** می‌کند نه از درخت React حذف
   می‌کند (حذف نودی که React مالکش است ناامن است). لنگر اول، شناسه‌های باثباتِ خودِ
   برنامه است — `#pattern-overlay-canvas-card` و `#pattern-stats-cards-card` — و اگر
   روزی تغییر نام دادند، تطبیقِ **دقیقِ متنِ عنوان** (فارسی یا انگلیسی) به‌عنوان
   پشتیبان کار می‌کند: نزدیک‌ترین والدِ دارای `canvas` یا کلاسِ کارت `display:none` +
   `hidden` می‌شود. اگر برنامه بعداً همان پنل‌ها را دوباره بسازد، دوباره
   مخفی می‌شوند. بقیه دست نمی‌خورد: «خلاصه روند»، «دیتا و الگوها»، هدر، و هیچ‌وقت
      مخفی نمی‌شود (گارد `tooBig`).
4. **دکمهٔ شناور/چسبان خودِ افزونه** (`#ohlc-tool` با `#ohlc-open`) هم حذف شد؛ دیگر
   چیزی به صفحهٔ برنامه اضافه نمی‌شود (بخش بالا را ببینید).
5. **بلوک آپلود در مرکز «محیط الگو» (همهٔ نوشته‌های وسطش، با عملکردشان)** —
   `#image-cropper-card` تا وقتی تصویری داخلش نباشد، یک بومِ وسط‌چین با سه چیز
   نشان می‌دهد: یک جعبهٔ آیکون، «تصویر چارت را بکشید و اینجا رها کنید» و زیرش
   «کادر زرد رنگ را روی بخش مورد نظر از نمودار تنظیم کنید». خودِ همان بلوک
   `onClick` دارد و کارش این است: `P.current.click()` — یعنی همان «کلید آپلود عکس»
   همین بلوک است (دکمهٔ جدا ندارد). در `chart-dna-ui-trim.js`:
   - بلوک `display:none` + `hidden` + `aria-hidden` می‌شود، پس هر سه نوشته از صفحه
     بیرون می‌روند (آیکون داخل همان بلوک است، پس با آن می‌رود)؛
   - عملکردش هم می‌میرد: هر `input[type=file]` داخل کارت `disabled` می‌شود و
     `click()` روی آن بی‌اثر شده (توابع نسخهٔ اصلی برای برگرداندن ذخیره می‌شود)، و
     یک شنوندهٔ capture روی خودِ کارت کلیک‌هایِ رسیده به آن بلوک و همچنین
     `dragover/dragenter/drop` را می‌خورد؛
   - **این کارت هیچ drop handler‌ای در باندل ندارد** (شمارش `onDrop` در
     `assets/index-DOCOgEgh.js` = ۰)؛ یعنی جملهٔ «بکشید و رها کنید» از اول فقط
     متن بوده و تنها مسیر واقعی، همان کلیک بود — که حالا بسته شده؛
   - دست‌نخورده‌ها: بومِ `canvas` و کادر زرد برش، ردیف بالای کارت (عنوان
     «محیط الگو»، «قیمت مرجع»، دکمهٔ «تنظیم») و نوار وضعیت پایین کارت. اگر بعداً
     تصویری از راه دیگری داخل کارت بیاید، همهٔ همان‌ها سر جایشان کار می‌کنند؛
   - `window.ChartDnaUiTrim.cropOff()` / `cropOn()` همین را خاموش/روشن می‌کنند و
     `chartdna_crop_upload = '0'` در localStorage نسخهٔ ماندگاراش است.

   پیامدش را صریح بگوییم: از این پس **هیچ مسیری برای ورود تصویر به خودِ «محیط
   الگو» از داخل همان کارت وجود ندارد** (سربرگ «DNA Vision v2» در تنظیمات یک جریان
   جداست: الگو را در کتابخانه ثبت می‌کند، نه تصویرِ داخل کارت). تصویر برای
   اندازه‌گیری OHLC در پنل خودمان انتخاب می‌شود — همان‌جا که کلید نوار پخش باز می‌کند.
6. **چهار کلیدِ خودِ پنجرهٔ «ورود تصویر» و همهٔ اتصالاتشان** — «📷 انتخاب تصویر از
   حافظهٔ دستگاه»، «استفاده از تصویری که در برنامه آپلود کرده‌اید»، «ثبت به‌عنوان دیتاست
   Chart DNA»، «ثبت و اجرای جستجو»؛ با جعبهٔ «اتصال به موتور Chart DNA» (دو تیک)،
   تابع‌های `saveDataset`/`appendPattern`/`openDb`/`reloadPage` و هندلر درون‌خطی `#ohlc-grab`، درهای
   `ChartDnaOhlc.saveDataset` و `.reload`، و بلوک `autoSearch`. جزئیات و نحوهٔ
   برگرداندن در بخش «اتصال به برنامه: حذف شد» بالا.
7. **سه پنلِ ستون راستِ پنجرهٔ «ورود تصویر»** — «برچسب دیتاست»، «کالیبراسیون محور قیمت»
   و «خروجی» — با کلیدها، ورودی‌ها، ذخیرهٔ localStorage و توابعشان؛ جزئیات در بخش
   «سه پنل کناریِ پنجره حذف شد».

**دادهٔ باقی‌مانده هم یک‌بار جارو می‌شود** (بعد از آن `chartdna_ui_trim_swept`
نوشته می‌شود تا تکرار نشود): دیتاست‌هایی که ما ساخته بودیم (`id` با پیشوند `img-` یا
نامِ «from image»/«کادر برنامه») از `market_datasets`، همان‌ها از
`chartdna_saved_patterns`، همهٔ کلیدهای `chartdna_ohlc_*` از localStorage، برداشتن آن
شناسه‌ها از `chartdna_selected_dataset_ids`، و حذف `chartdna_reference_price` **فقط**
اگر همان عددی باشد که ما نوشته بودیم. دادهٔ خودِ کاربر (دیتاست‌ها و الگوهای دیگر)
دست نمی‌خورد و اگر جایی خوانده نشود، هیچ حذفی انجام نمی‌شود.

**خاموش/روشن:** `localStorage.chartdna_ui_trim = '0'` → کل این اسکریپت هیچ کاری
نمی‌کند. `localStorage.chartdna_crop_upload = '0'` → فقط مورد ۵ (مرکز «محیط الگو»)
به برنامه برگردانده می‌شود. **برگرداندن قابلیت خودکار:**
`git checkout <commit> -- chart-ohlc-autopilot.js` و سپس یک تگ
`<script src="chart-ohlc-autopilot.js?v=N">` به `index.html` و همان فایل به
`PRECACHE_URLS` در `sw.js` (نسخهٔ `d5ce374` آخرین حالت کاملش بود).

ابزار **دستی** سرجایش است: پنل استخراج که با همان کلید «ورود تصویر چارت»
(یا `window.ChartDnaOhlc.open()`) باز می‌شود
(جدول کندل‌ها، «دانلود CSV» و «دانلود تصویر حاشیه‌نویسی‌شده»)؛ فقط نسخهٔ خودکار و
آن پنجره‌ها حذف شدند (مورد ۵ هم که فقط مخفی‌شدنِ یک بلوک است، هر دو طرفش در تست‌ها
چک می‌شود). دیگر **هیچ دری برای نوشتن در برنامه باز نیست** — نه دکمه‌ای، نه
تابعی روی `window.ChartDnaOhlc`؛ `saveDataset` هم از اوندرورفت.

## ردیف شش‌کلیدیِ هم‌فرمتِ نوار پخش (بالای پنجرهٔ «ورود تصویر»)

`#ohlc-bar` کپیِ همان ردیفی است که در صفحهٔ اول، زیر کارت «محیط الگو» ایستاده
(`#remote-control-deck`): همان ظرف (`flex items-center justify-between gap-1 p-1.5
rounded-xl border`) و روی هر کلید **همان رشتهٔ کلاسِ** `#btn-import-image`:
`flex-1 h-9 rounded-lg flex items-center justify-center transition-all duration-150
border cursor-pointer active:scale-95 group relative`. همان‌جا هم کلیدها فقط آیکون‌اند؛
پس اینجا هم **فقط آیکون، بدون نوشته** — و برای اینکه ظاهر به Tailwind وابسته نماند،
همان اعداد (۳۶px قد، گوشهٔ ۸px، فاصلهٔ ۴px) در CSS خودِ پنل هم نوشته شده‌اند.

آیکون‌ها هم از همان کتابخانه‌اند: **lucide-react v0.475.0** — دادهٔ گره‌های هر گلیف از
خودِ باندل برنامه برداشته و به‌صورت SVG درون‌خطی شده (`<svg class="lucide lucide-…"
viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">`)، با همان
`w-4 h-4 group-hover:scale-110 transition-transform` که کلیدهای برنامه دارند:

| # | آیکون (lucide) | کلید | کار |
|---|---|---|---|
| ۱ | `image` | `#ohlc-drop` | انتخاب تصویر / رها‌کردن فایل / Ctrl+V |
| ۲ | `scan-line` | `#ohlc-run` | «استخراج کندل‌ها» روی تصویرِ انتخاب‌شده |
| ۳ | `chart-no-axes-column` | `[data-view=chart]` | کندل‌های بازسازی‌شده (همان قاب، همان جای کندل‌ها) |
| ۴ | `eye` | `[data-view=orig]` | اسکرین‌شات، بدون هیچ علامتی |
| ۵ | `crosshair` | `[data-view=ann]` | همان اسکرین‌شات + کادرهای شماره‌گذاری‌شده، خط‌های کالیبراسیون… |
| ۶ | `circle-check` | `#ohlc-confirm` | **تأیید** و بازگشت به صفحهٔ اصلی برنامه |

- **هیچ متنی روی کلیدها نیست**، ولی هیچ جمله‌ای هم دور ریخته نشده: هر کلید همان جمله را در
  `title` و `aria-label` دارد (تولtip در دسکتاپ، و صفحه‌خوان‌ها هم همان را می‌خوانند).
  `#ohlc-file` هم عمداً بیرونِ ردیف و به‌عنوان خواهرِ آن نشسته تا داخل دکمه input نباشد.
- **`کلید ششم = تأیید`:** تا وقتی چیزی استخراج نشده `disabled` است (همان کاری که کلیدهای
  خودِ برنامه با `disabled:!g` می‌کنند). یک فشارش، روند را تأیید‌شده علامت می‌زند
  (`state.confirmedKey` + `confirmedAt`/`confirmedCandles` در `window.__ohlcReport`) و
  پنجره را می‌بندد؛ پشتِ همین پنجره صفحهٔ اول برنامه است، پس کاربر همان‌جا می‌ایستد.
  **هیچ نوشتنی در برنامه انجام نمی‌شود** — تست `3b` همان‌جا `IndexedDB opens=0` و
  `search clicks=0` را می‌سنجد. نتیجهٔ استخراج و جدول سرِ جایشان می‌مانند و با
  `ChartDnaOhlc.open()` دوباره باز می‌شوند.
- **`بستن` از پایین کارت حذف شد** (کلید و رشته‌اش با هم) — همان کلید حالا ششمین کلیدِ این
  ردیف است با نام «تأیید». پنجره همچنان با Escape یا کلیک روی پس‌زمینهٔ تیره بسته می‌شود،
  و `ChartDnaOhlc.close()` هم کنار `open()` اضافه شد.
- **مکانیزم «دو خط / فقط آیکون» نسخهٔ قبل برداشته شد** (`fitBar`، `.ohlc-lbl`،
  `ohlc-1/ohlc-2/ohlc-ic`، `ResizeObserver`ِ بازسنجی): وقتی کلیدها از اول آیکون‌اند،
  اندازه‌گیریِ جا‌شدنِ متن معنایش را از دست می‌دهد. این‌ها حذف شدند، نه مخفی — و `.ohlc-primary`
  و `.ohlc-danger` هم از CSS رفتند، چون تنها مصرف‌کننده‌هایشان همان کلیدهای «استخراج» مربعی و
  «بستن» بودند.

## یک قاب برای هر سه نما

قبلاً کادر «کندل‌های بازسازی‌شده» یک بوم ۱۰۰۰×۳۰۰ بود (در CSS هم `height:300px` قفل شده)
و «تصویر اصلی» و «تصویر با مارک‌ها» هر کدام اندازه‌ای از خودشان داشتند — برای همین
درازا و قد این نماها با هم نمی‌خواند. حالا **یک قاب** مشترک است:

- `frameBox()` در `chart-ohlc-extractor.js` قاب را از خودِ اسکرین‌شات می‌گیرد و ضلع بلندش
  را حداکثر ۱۴۰۰ پیکسل می‌کند (مثلاً ۲۷۱۲×۱۲۲۰ ← ۱۴۰۰×۶۳۰).
- `paintInto()` همان قاب را به هر سه بوم می‌دهد: `#ohlc-chart`، `#ohlc-orig`، `#ohlc-ann` —
  پس عرض، ارتفاع و نسبتِ هر سه **یکی** است و هیچ‌کدام کشیده نمی‌شود
  (یک قاعدهٔ CSS برای هر سه: `width:100%;height:auto`).
- `renderReconstructed` کندل‌های بازسازی‌شده را در **فضای پیکسلی همان تصویر** می‌کِشد:
  هر کندل دقیقاً روی ستون و ردیفی که اندازه گرفته شده می‌نشیند (`b.x`, `rowHigh/rowLow`,
  `rowBodyTop/rowBodyBot`, پهنای بدنه از گام شبکه) — بدون auto-fit و بدون مقیاس‌ مجدد؛
  کندل‌های خوانده‌نشده مثل نمای مارک‌دار با کادر قرمز علامت می‌خورند و رنگ
  صعودی/نزولی همان تیل و قرمز همیشگی است.
- نتیجه: اگر سه نما را یکی‌یکی عوض کنید، مجموعهٔ کندل‌ها جا عوض نمی‌کند — شباهت (و
  اختلاف) چشمی قابل مقایسه است. تست `ui` این را با پیکسل می‌سنجد: ۲۵۷/۲۵۷ کندلِ با‌اطمینان
  در نمای مارک‌دار، دقیقاً روی همان پیکسلی علامت دارند که نمای بازسازی‌شده شدو کشیده است.
- **ریتم کارت‌ها**: در پهنای ≥۸۲۰px، کادرِ هر سه نما `min-height:340px` و `max-height:390px`
  می‌گیرد — همان ارتفاعی که خودِ برنامه به کارت‌های چارتش می‌دهد (`h-[340px] md:h-[390px]` روی
  `#comparative-chart-card` و `#pattern-overlay-canvas-card`). چون ما را هم با همان
  `object-fit:contain` تنظیم کردیم، ارتفاع کارت‌ها در صفحه یک‌دست است و تصویر هم کشیده نمی‌شود.
  این را روی **گره‌های خودمان** انجام دادیم، نه با استایل دادن به کارت‌های React: آن کارت‌ها
  خودشان با `ResizeObserver` بومشان را به اندازهٔ کادر می‌سازند و اگر نسبتِ ما را به آن‌ها
  تحمیل می‌کردیم، یا با resize خودشان می‌جنگیدیم یا شکلِ نمودارِ خود برنامه عوض می‌شد.
- چون تصویر با `object-fit:contain` وسط کادر نشسته، کلیکِ «افزودن نقطهٔ مرجع روی محور قیمت»
  هم روی **خودِ تصویر** نگاشت می‌شود (`contentBox()`): اگر انگشت روی حاشیهٔ خالیِ کادر بخورد،
  پنجره می‌گوید «روی خودِ تصویر کلیک کنید» و ردیفی از هیچ می‌سازد **نیست**.
- قابِ نمایش ۱۴۰۰px است تا گوشی سه بوم تمام‌رزولوشن نگه ندارد، ولی **فایل PNG** هنگام
  دانلود دوباره در رزولوشن کاملِ تصویر رندر می‌شود (۲۷۱۲×۱۲۲۰) — یعنی هم‌اندازه‌کردن نماها
  به قیمت افتِ کیفیت فایل تمام نمی‌شود.

## سه پنل کناریِ پنجره حذف شد (برچسب دیتاست · کالیبراسیون · خروجی)

پنجرهٔ «ورود تصویر» دو ستون بود: قاب نماها در چپ، و در راست سه جعبه —
**«برچسب دیتاست»** (نماد، تایم‌فریم، تاریخ شروع/پایان، ساعت شروع)،
**«کالیبراسیون محور قیمت»** (متن راهنما، ردیف و قیمت مرجع، «افزودن»، «پاک کردن»، فهرست
مراجع) و **«خروجی»** (گزارش چندخطی زیر تیترش). هر سه با کلیدهایشان حذف شدند —
نه مخفی‌سازی: چیزی که ساخته نمی‌شود، جارو هم نمی‌خواهد.

**چه رفت (در `chart-ohlc-extractor.js`، نسخهٔ API = ۱۴):**
- مارک‌آپ: `#ohlc-symbol`، `#ohlc-tf`، `#ohlc-d0`، `#ohlc-d1`، `#ohlc-t0`، `#ohlc-ref-row`،
  `#ohlc-ref-price`، `#ohlc-ref-add`، `#ohlc-ref-clear`، `#ohlc-points` و هر سه
  `<div class="ohlc-box">`؛
- حالت: `state.points` از `state` بیرون رفت؛ `renderPoints`؛ و کلیک روی تصویر اصلی برای
  گرفتن «ردیف مرجع» (با نگاشت `contentBox` به نوار letterbox) — آن کلیک دیگر هیچ نقطه‌ای
  جمع نمی‌کند؛
- ذخیره‌سازی: `FORM`، `restoreForm`، شنونده‌های `change` و کلید `chartdna_ohlc_form` —
  هیچ ورودی‌ای نمانده که ذخیره‌اش کنند؛
- مسیرها: `extraRefs` از `run()` حذف شد (موتور نبودنش را می‌پذیرد)، تابع `applyDates` با
  کل بدنه‌اش رفت، و `report()` دیگر `source: {symbol, timeframe}` نمی‌نویسد؛
- نوشته‌ها: `T.calibNote`؛ استایل‌ها: `.ohlc-grid`، `.ohlc-box` و فرزندهایش،
  `.ohlc-refrow`، قواعد `#ohlc-points …`، `.ohlc-kv`، `.ohlc-muted` و
  `@media(max-width:820px){.ohlc-grid…}`. ستون‌بندی دو‌تایی جایش را به یک ستون تمام‌عرض
  داد، پس قاب نماها دیگر عرضش را با هیچ پنل دیگری تقسیم نمی‌کند؛
- `#ohlc-status` تنها بازماندهٔ آن ستون است و جعبه و تیتر «خروجی» را ندارد: یک خط کوتاه
  زیر جدول با گزارش فشرده (تعداد کندل‌ها، مدل محور، ۱ پیکسل ≈ چند دلار، چند کندل نیازمند
  بازبینی، و خطا/هشدار). متن کامل جزئیات همچنان در `window.__ohlcReport` است — در
  حافظه، برای تست و دیباگ، نه روی صفحه.

**پیامدها، صریح:**
- `Date`/`Time` در CSV همیشه خالی است (بخش «تاریخ و ساعت»)؛
- **زنجیرهٔ پشتیبانِ دستیِ محور رفت:** اگر خواندن خودکار محور قیمت شکست بخورد، دیگر نمی‌توان
  ردیف/قیمت مرجع دستی داد. پنجره همان جملهٔ «محور قیمت خوانده نشد → Open/High/Low/Close
  خالی می‌ماند» را نشان می‌دهد و اعداد را **نمی‌سازد** — قاعدهٔ همیشگی: چیزی که در
  پیکسل‌ها نیست، در CSV نیست؛
- نام فایل CSV از «نماد» به `chart_ohlc_from_image.csv` عوض شد (نام‌گذاری از فیلد
  نماد می‌آمد)؛
- خط‌های ترازِ کالیبراسی روی «تصویر با مارک‌ها» دست‌نخورده‌اند: از `result.calibration`
  خوانده می‌شوند، نه از ورودی کاربر.

**جاروی نسخهٔ قدیمی:** `chart-dna-ui-trim.js` (نسخهٔ ۴) همان ده شناسه را به `STALE` اضافه
کرد تا اگر اسکریپت قدیمی در کش کسی هنوز آن جعبه‌ها را می‌سازد، از صفحه برداشته شوند
(کرانهٔ هر شناسه ۵ بار)؛ `chartdna_ohlc_form`ِ باقی‌مانده هم در جاروی داده پاک می‌شود.

**برگرداندن:** `git checkout 920c5e1 -- chart-ohlc-extractor.js` و پایین بردن `?v=` در
`index.html` و `sw.js` (و `version: 3` در `chart-dna-ui-trim.js`).

## محدودیت‌ها

دقت به گام ۱ پیکسل محدود است (این چارت: ≈۰.۵۵ دلار بر پیکسل)؛ در نواحی شلوغ که بدنه‌ها
روی هم می‌افتند مرزها فقط تا دقت همان پنجره قابل تفکیک‌اند؛ حجم استخراج نمی‌شود؛
برچسب‌های ساعتِ تک‌تک کندل‌ها در این سبک چارت وجود ندارند؛ خروجی یک بازسازی هندسی است،
نه تحلیل تکنیکال. هم‌اندازه‌سازی شامل سه نمای همین پنجره است — بومِ چارتِ خودِ برنامه
(کارت‌های React، مثل «کشف الگو در تاریخچه») را ما نمی‌کِشیم و دست به استایلش نمی‌بریم؛
اگر هم‌قاب‌کردن آن هم خواسته شود، تصمیم جداگانه‌اش لازم است.
