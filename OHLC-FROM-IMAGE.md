# استخراج OHLC از تصویر نمودار (Image → Candles)

سه اسکریپت در ریشهٔ مخزن، بدون بیلد و بدون وابستگی شبکه:

| فایل | نقش |
|---|---|
| `chart-ohlc-engine.js` | موتور بینایی (`window.ChartDNACV`) — کالیبراسیون محور قیمت، یافتن شبکهٔ کندل‌ها، اندازه‌گیری بدنه/شدو، اطمینان، CSV، رندر |
| `chart-ohlc-extractor.js` | پنل «📈 استخراج OHLC از تصویر» (دکمهٔ چسبان بالای صفحه) و اتصال نتیجه به Chart DNA |
| `chart-dna-ui-trim.js` | برداشتن دو پنجره‌ای که قرار بود نباشند + جاروکردن دادهٔ باقی‌مانده (پایین همین سند) |

`index.html` هر سه را با `?v=10` بارگذاری می‌کند و `sw.js` آن‌ها را در `PRECACHE_URLS` دارد
(نسخهٔ کش: `chartdna-v2.10.0-remove-ohlc-card-and-overlay`). برای انتشار مجدد فقط `?v=` را بالا ببرید.

## چه کاری انجام می‌دهد (و چه کاری نمی‌کند)

فقط پیکسل‌ها خوانده می‌شود؛ هیچ دادهٔ قیمتی از اینترنت یا «حافظهٔ مدل» نمی‌آید:

1. ماسک رنگی کندل‌های صعودی/نزولی؛ جداکردن اجزای رابط کاربری (تیکر هدر، برچسب قیمت،
   پنل حجم، آیکون‌ها) بر پایهٔ ریخت‌شناسی — نه مستطیل از پیش تعریف‌شده.
2. بازیابی شبکهٔ کندل: جست‌وجوی فاز/گام روی مرکزِ بدنه‌ها + رگرسیون کمترین مربعات،
   رتبه‌بندی با «پوشش یکنواخت روی کل پهنای پلات».
3. کالیبراسیون محور عمودی: خطوط راهنما از اختلاف روشنایی پس‌زمینه (با فیلتر ضد دیتر)،
   اعداد محور با OCR رقمی بدون وابستگی خارجی (قالب ۱۴×۲۰، همبستگی نرمال‌شده)،
   مقایسهٔ مدل خطی و لگاریتمی، و آزمون مستقل با برچسب قیمتِ روی محور.
4. برای هر کندل: بدنه از ردیف‌هایی که تقریباً کل پنجرهٔ ±۲ پیکسلی را پر کرده‌اند،
   High/Low از انتهاهای جزء به هم پیوستهٔ همان کندل (حلقه‌های متصل)، ترمیم ردیف‌های
   پوشیده‌شده توسط خط‌چین قیمت آخر.
5. اطمینان هر کندل از قوانین داخلى (O/H/L/C)، پیوستگی close→open با همسایه، ضخامت بدنه،
   مرز پنل و ابهام رنگ. هر چه اندازه‌گیری نشود **خالی** می‌ماند، نه حدس.

## اتصال به برنامه

* دیتاست در IndexedDB `ChartDNA_Storage` / store `market_datasets` با همان شکل
  `{id,name,symbol,timeframe,candles:[{timestamp,open,high,low,close,volume}]}` نوشته می‌شود.
* شناسه به `chartdna_selected_dataset_ids` **افزوده** می‌شود (با گزینهٔ «فقط این یکی» قابل جایگزینی).
* سری قیمت بسته‌شدن به‌عنوان یک الگو به `chartdna_saved_patterns` اضافه می‌شود تا موتور
  جست‌وجوی DNA آن را بین دیتاست‌ها بجست‌وجو کند. اگر برنامه هنوز کتابخانه‌اش را در
  localStorage نساخته باشد، الگو در sessionStorage صف می‌شود و در بارگذاری بعدی
  **بدون حذف الگوهای داخلی** الحاق می‌گردد.
* «ثبت و اجرای جستجو» پرچم `chartdna_ohlc_autosearch` را می‌گذارد، صفحه ریلود می‌شود و
  پس از آماده‌شدن UI، دکمهٔ «جستجو» برنامه یک بار کلیک می‌شود.
* تاریخ/ساعت: فقط از لنگرهایی که کاربر می‌دهد (دو تاریخ، یا تاریخ+تایم‌فریم) یا از
  خطوط عمودی محور محاسبه می‌شود؛ اگر هیچ لنگره‌ای نباشد `Date`/`Time` خالی می‌ماند.

## اعتبارسنجی

موتور روی همان اسکرین‌شات XAUUSD 1h که با پایتون/NumPy استخراج و بازبینی شده بود اجرا شد
(تست‌های `dev-tests` در محیط توسعه): ۲۹۶ کندل، **۹۹.۶٪** از ۱۱۸۴ سطح OHLC اختلافشان با
خروجی پایتون کمتر از یک پیکسل (≤۰.۶۱ دلار) بود، میانهٔ اختلاف ۰.۲۲ دلار، صفر اختلاف در
جهت کندل، و تاریخ‌ها ۲۹/۲۹۶ یکسان. خطای آزمون مستقلِ برچسب قیمت ۰.۰۱۶ دلار.

## دکمهٔ استخراج، چسبان در بالای صفحه

دکمهٔ «📈 استخراج OHLC از تصویر» دیگر گوشهٔ پایینِ صفحه شناور نیست. به
`position: fixed` بالای صفحه میخکوب می‌شود:

- افقی: به لبهٔ راستِ ستونِ برنامه (`#chart-dna-app`) تراز می‌شود، پس در چیدمان
  راست‌به‌چپ هم سرِ جای درستِ خودش است؛ اگر عرض صفحه کم باشد داخل viewport
  نگه داشته می‌شود.
- عمودی: درست **زیر نوار بالای برنامه** (به اندازهٔ ارتفاع دکمهٔ `تنظیمات` + ۸ پیکسل)
  می‌نشیند، پس نه لوگو را می‌پوشاند و نه دکمهٔ تنظیمات را. با اسکرول کردن که نوار بالا
  از دید رفت، به `top: 12px` می‌چسبد و همیشه در دسترس می‌ماند.
- شمار کندل‌های استخراج‌شده داخل خود دکمه نوشته می‌شود (`۲۹۶ کندل`) تا وضعیت کار
  بدون باز کردن پنل دیده شود؛ وقتی پنل استخراج باز است دکمه پشت overlay پنهان می‌شود.
- جابه‌جایی بی‌صدای React با یک بررسی هندسهٔ ۴۰۰ میلی‌ثانیه‌ای و هم‌چنین
  `scroll`/`resize`/`orientationchange` (با `capture`، پس اسکرول کانتینر داخلی هم دیده
  می‌شود) دنبال می‌شود.
- کلید خاموش/روشن: `localStorage.chartdna_ohlc_pin` (`0` = همان شناورِ قدیمیِ پایین
  صفحه).

## چه چیزی حذف شد (و چطور برگردانده می‌شود)

در `chartdna-v2.10.0-remove-ohlc-card-and-overlay` دو پنجره از برنامه برداشته شد:

1. **کارت «کندل‌های بازسازی‌شده از همین تصویر (OHLC از پیکسل)»** — کل
   `chart-ohlc-autopilot.js` حذف شد، یعنی: استخراج خودکار از تصویری که کاربر در
   «محیط الگو» آپلود می‌کند، رسم کندل‌ها در محیط برنامه، ثبت خودکار دیتاست، افزودن
   خودکار الگو به کتابخانه، هم‌گام‌سازی «قیمت مرجع بازار»، دنبال‌کردن کادر زردِ برش و
   بازخوانی/کلیک خودکار «جستجوی فوری» DNA.
2. **پنل «رسم چارت و سطوح تحلیل الگو» (Pattern Chart Overlay) خودِ برنامه** — با
   سطوح حمایت/مقاومت، تارگت‌ها و برچسب قیمتش. این پنل در باندل مینیفای‌شده
   (`assets/index-DOCOgEgh.js`) است و سورسی در این مخزن ندارد، پس
   `chart-dna-ui-trim.js` آن را **مخفی** می‌کند نه از درخت React حذف کند (حذف نودی که
   React مالک آن است ناامن است). عنوان را دقیقاً تطبیق می‌دهد (فارسی یا انگلیسی) و
   نزدیک‌ترین والدِ دارای `canvas` یا کلاس کارت را `display:none` + `hidden` می‌کند؛
   اگر برنامه بعداً همان پنل را دوباره بسازد، دوباره مخفی می‌شود. پنل همسایه
   («کارت‌های آماری تحلیل الگو») و هدر دست نمی‌خورند، و هیچ‌وقت کل `#chart-dna-app`
   مخفی نمی‌شود.

**دادهٔ باقی‌مانده هم یک‌بار جارو می‌شود** (بعد از آن `chartdna_ui_trim_swept`
نوشته می‌شود تا تکرار نشود): دیتاست‌هایی که ما ساخته بودیم (`id` با پیشوند `img-` یا
نامِ «from image»/«کادر برنامه») از `market_datasets`، همان‌ها از
`chartdna_saved_patterns`، همهٔ کلیدهای `chartdna_ohlc_*` از localStorage، برداشتن آن
شناسه‌ها از `chartdna_selected_dataset_ids`، و حذف `chartdna_reference_price` **فقط**
اگر همان عددی باشد که ما نوشته بودیم. دادهٔ خودِ کاربر (دیتاست‌ها و الگوهای دیگر)
دست نمی‌خورد و اگر جایی خوانده نشود، هیچ حذفی انجام نمی‌شود.

**خاموش/روشن:** `localStorage.chartdna_ui_trim = '0'` → کل این اسکریپت هیچ کاری
نمی‌کند. **برگرداندن قابلیت خودکار:**
`git checkout <commit> -- chart-ohlc-autopilot.js` و سپس یک تگ
`<script src="chart-ohlc-autopilot.js?v=N">` به `index.html` و همان فایل به
`PRECACHE_URLS` در `sw.js` (نسخهٔ `d5ce374` آخرین حالت کاملش بود).

ابزار **دستی** سرجایش است: دکمهٔ «📈 استخراج OHLC از تصویر» بالای صفحه و پنل آن
(جدول کندل‌ها، CSV، تصویر حاشیه‌نویسی‌شده، «ثبت دیتاست»، «ثبت و اجرای جستجو»);
فقط نسخهٔ خودکار و آن دو پنجره حذف شدند. تابع `saveDataset(..., {pattern:true})`
هم در `window.ChartDnaOhlc` باقی مانده تا اگر خواستید، الگو را دستی به کتابخانه
بدهید — اما هیچ دکمه‌ای در UI دیگر آن را صدا نمی‌زند.

## محدودیت‌ها

دقت به گام ۱ پیکسل محدود است (این چارت: ≈۰.۵۵ دلار بر پیکسل)؛ در نواحی شلوغ که بدنه‌ها
روی هم می‌افتند مرزها فقط تا دقت همان پنجره قابل تفکیک‌اند؛ حجم استخراج نمی‌شود؛
برچسب‌های ساعتِ تک‌تک کندل‌ها در این سبک چارت وجود ندارند؛ خروجی یک بازسازی هندسی است،
نه تحلیل تکنیکال.
