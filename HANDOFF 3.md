# Hand-off — پروژه «شورای هوش مصنوعی» (Ais Mittings / AI Boardroom)

این سند برای هوش مصنوعی یا توسعه‌دهنده‌ای نوشته شده که قراره ادامه‌ی این پروژه‌ی اندروید (Kotlin + Jetpack Compose) رو بگیره. لطفاً قبل از هر تغییری، کل این سند رو بخونید — چند بخشش صراحتاً می‌گه چه چیزهایی **نباید** بدون هماهنگی با کاربر (رییس جلسه / صاحب پروژه) تغییر کنن.

---

## ۰. وضعیت فعلی به‌طور خلاصه (بخون قبل از هرچیز دیگه)

- **کد کجاست:** ریپوی GitHub خصوصی/عمومی `reza0043/chart-dna` (نام ریپو گمراه‌کننده است — این پروژه، پروژه‌ی «AI Boardroom» است، نه پروژه‌ی «Chart DNA»؛ کاربر یک ریپوی دیگه هم به همین نام برای پروژه‌ی جداگانه‌ی خودش داره، اشتباه نگیرید).
- **Build واقعی تأییدشده ✅:** یک GitHub Actions workflow به اسم **«Build AI Boardroom APK»** (فایل `build-apk.yml`, on: push) روی این ریپو تعریف شده. اولین اجرا (run id `32799271130`) با موفقیت کامل (Status: Success، ۳ دقیقه و ۴۷ ثانیه) یک `app-debug.apk` واقعی (~۱۸.۶ مگابایت) تولید کرد. یعنی **تمام کد فازهای ۱ تا ۸ زیر، واقعاً کامپایل می‌شه** — این دیگه یک ریسک باز نیست.
- **برای ادامه‌ی کار:** روی همون سورس‌کد داخل ریپو ویرایش کن، commit/push کن، و همین workflow خودش APK جدید می‌سازه (خروجی در تب Actions › آخرین run › Artifacts).
- **آخرین قابلیت تکمیل‌شده:** فاز ۸ — مقصد ذخیره‌سازی انتخابی حافظه (Local/GitHub/Google Drive)، جزئیات کامل در بخش ۷.
- **مهم‌ترین چیزی که هنوز باز مونده:** کاربر هنوز مراحل یک‌بارهٔ خودش (ساخت GitHub PAT، ساخت پروژه‌ی Google Cloud برای Drive OAuth) رو انجام نداده — بدون این‌ها، بخش GitHub/Drive این فاز عملاً قابل‌تست نیست ولی کدش کامله. بند ۴.۶ رو ببین.
- **قوانین سخت‌گیرانه‌ای که باید رعایت کنی:** چیدمان UI (بند ۱.۴)، بدون فایل یتیم در ریشه‌ی پروژه، مدل امنیتی کلید API/توکن (بند ۵) — این‌ها رو نقض نکن مگر با تأیید صریح کاربر.

---

## ۱. پرامپت اصلی پروژه — نسخه‌ی اصلاح‌شده و دقیق

کاربر یک اپلیکیشن اندروید چت می‌خواد که در اون کاربر به‌عنوان **رییس جلسه**، یک مسئله رو با ۲۰ «کارگروه مشاور» مطرح می‌کنه. هر کارگروه خودش ۵ زیرمجموعه (مدل هوش مصنوعی) داره. مشخصات دقیق و لازم‌الاجرا:

### ۱.۱ ساختار مشاوران
- **۲۰ کارگروه مشاور**، هرکدام دارای نام، رنگ و آیکون قابل‌ادیت.
- هر کارگروه **۵ جایگاه زیرمجموعه** داره. در هر جایگاه کاربر با API شخصی خودش می‌تونه یک مدل هوش مصنوعی رو «استخدام» کنه: نام اختصاصی، نوع/نام مدل (Gemini, GPT-4o, Claude, Ollama محلی و ...)، کلید API مخصوص، و در صورت نیاز آدرس CLI/سرور محلی (برای مدل‌های خوداستقرار مثل Ollama).
- همه‌ی اعضایی که رییس جلسه اجازه‌ی حضورشون رو داده، باید کل گفتگوی چت‌روم رو کامل ببینن و در جریان باشن.

### ۱.۲ چهار حالت تصمیم‌گیری رییس جلسه (Dispatch Modes)
1. **تشخیص خودکار (Auto Triage)** — یک کارگروه مسئول (پیش‌فرض کارگروه ۲۰)، مسئله رو تحلیل و به کارگروه‌(های) مرتبط ارجاع می‌ده.
2. **انتخاب دستی (Selective)** — خود کاربر تعیین می‌کنه کدام کارگروه‌ها شرکت کنن.
3. **جلسه علنی (Public Assembly)** — همه‌ی ۲۰ کارگروه در بحث شرکت و نظر می‌دن.
4. **زنجیره‌ای/پیوسته (Sequential Pipeline)** — خروجی هر کارگروه، ورودی کارگروه بعدی در توالی مشخص‌شده می‌شه.

### ۱.۳ حافظه‌ی داخلی
- مسیر حافظه از تنظیمات کاربر قابل تنظیمه.
- برای هر جلسه (بر اساس عنوان صورت‌جلسه) یک **پوشه‌ی مخصوص همون جلسه** ساخته می‌شه که فایل، صدا، متن و خروجی‌های تولیدشده در اون جلسه داخلش ذخیره می‌شه.
- یک پوشه‌ی **`master`** مشترک وجود داره که تمام مدل‌های هوش مصنوعی ثبت‌شده در برنامه بهش دسترسی دارن (اطلاعات کلی/سراسری پروژه).
- در ابتدای هر اجرا/جلسه، برنامه باید حافظه‌ی خودش (master + خلاصه‌ی جلسات قبلی) رو مرور کنه تا همه‌ی اعضا از روند کلی گفتگوها و نتایج قبلی باخبر باشن.

### ۱.۴ چیدمان صفحه (UI) — **نسخه‌ی تأییدشده توسط کاربر**
> ⚠️ این چیدمان توسط کاربر بررسی و **صراحتاً تأیید و قفل شده** (فاز ۷ که چیدمان رو به دو ستون چپ/راست تغییر می‌داد، به درخواست کاربر لغو شد). چیدمان فعلی **نباید** بدون درخواست صریح کاربر تغییر کنه:
- یک نوار افقی بالای صفحه با ۱۰ پنجره (کارگروه‌های ۱ تا ۱۰).
- یک نوار افقی پایین صفحه با ۱۰ پنجره (کارگروه‌های ۱۱ تا ۲۰).
- هر پنجره: نام قابل‌ادیت، رنگ قابل‌ادیت، آیکون/لوگو قابل‌ادیت؛ با کلیک، گزارش خروجی همون کارگروه ۵‌نفره نمایش داده می‌شه.
- یک پنل چت مرکزی/یکپارچه (بالا = جریان پیام‌ها شامل پیام رییس جلسه و گزارش‌های مشاوران؛ پایین = نوار ورودی متن/صدا/فایل). این نسخه‌ی نهایی تأییدشده‌ست، نه دو ستون جدای چپ/راست که در پرامپت اولیه اومده بود.
- آیکون تنظیمات، گوشه‌ی بالا-چپ صفحه.
- تمام توضیحات فنی (روش استفاده از API هر مدل، راه‌اندازی Ollama/CLI محلی، اتصال MCP، توضیح هر دکمه و پنجره) فقط داخل صفحه‌ی تنظیمات قرار داره؛ صفحه‌ی اصلی برنامه از هرگونه توضیح اضافه خالیه.

### ۱.۵ محدودیت پلتفرم که باید به کاربر یادآوری بشه
عبارت «نصب مدل‌هایی با CLI» در پرامپت اولیه، به این معنا **نیست** که اپ اندروید بتونه از داخل sandbox خودش یک ابزار CLI رو نصب/اجرا کنه (این از نظر امنیتی روی اندروید ممکن نیست). پیاده‌سازی فعلی: کاربر خودش سرویس (مثل Ollama) رو روی یک PC/سرور در شبکه‌اش بالا می‌آره؛ اپ فقط به آدرس HTTP اون سرور (فیلد `cliOrEndpoint`) وصل می‌شه.

---

## ۲. وضعیت فعلی معماری (نقشه‌ی فایل‌ها برای جهت‌یابی سریع)

```
app/src/main/java/
├── com/example/
│   ├── MainActivity.kt              — چیدمان اصلی (نوار بالا/پایین ۱۰تایی + چت مرکزی)
│   ├── BoardroomApp.kt               — Application class: می‌سازه و نگه می‌داره:
│   │                                    database, memoryManager, secureKeyStore, repository
│   ├── data/
│   │   ├── local/
│   │   │   ├── AppDatabase.kt        — تعریف Room DB
│   │   │   └── MemoryManager.kt      — مدیریت پوشه‌ی master + پوشه‌ی هر جلسه روی دیسک
│   │   ├── model/
│   │   │   └── CouncilModels.kt      — Entityها (AdvisorEntity, MeetingSession, ChatMessage,
│   │   │                                MasterFile), SubAgentSlot، DispatchMode، و
│   │   │                                CouncilDataConverters (شامل subAgentsToJson —
│   │   │                                که کلید API رو قبل از ذخیره پاک می‌کنه — و
│   │   │                                parseTriageDecision برای تشخیص خودکار)
│   │   ├── network/
│   │   │   └── GeminiClient.kt       — کلاینت واقعی Gemini + fallback شبیه‌سازی‌شده (با برچسب هشدار)
│   │   └── repository/
│   │       └── BoardroomRepository.kt — مغز منطق کسب‌وکار: ۴ حالت دیسپچ، حافظه، ارکستراسیون
│   └── ui/
│       ├── components/
│       │   ├── AdvisorConfigDialog.kt — ادیت نام/رنگ/آیکون کارگروه + ۵ جایگاه زیرمجموعه
│       │   ├── AdvisorStrip.kt        — نوار ۱۰تایی بالا/پایین
│       │   ├── ChatPane.kt            — پنل چت (ارسال متن/صدا/فایل)
│       │   ├── NewSessionDialog.kt    — شروع جلسه‌ی جدید
│       │   └── SettingsDialog.kt      — تنظیمات + راهنمای API/CLI/MCP (طبق بند ۱.۴)
│       └── viewmodel/
│           └── BoardroomViewModel.kt  — پل بین UI و Repository/SecureKeyStore
├── ModelRouter.kt      (پکیج com.example.data.network) — روتر چند-مدلی؛ بر اساس
│                          modelType/endpoint به Gemini/OpenAI/Anthropic/Ollama/MCP می‌فرسته
├── AnthropicClient.kt, OpenAIClient.kt, OllamaClient.kt — کلاینت هر ارائه‌دهنده
└── SecureKeyStore.kt   — ذخیره‌ی رمزنگاری‌شده‌ی کلید API (Android Keystore-backed
                           EncryptedSharedPreferences)، کلید ذخیره‌سازی: (advisorId, slotNumber)
```

فایل‌های Gradle: `settings.gradle.kts` (ریشه)، `build.gradle.kts` (ریشه، فقط پلاگین‌ها)، `app/build.gradle.kts` (ماژول اصلی)، `gradle/libs.versions.toml` (کاتالوگ نسخه‌ها).

---

## ۳. کارهای انجام‌شده (فاز ۱ تا ۶) — چه‌کاری، چرا، کجا

| فاز | موضوع | خلاصه‌ی کار |
|---|---|---|
| **بررسی اولیه** | ممیزی کامل کد | پروژه‌ی اولیه‌ی دریافتی از AI Studio بررسی و باگ‌های ریشه‌ای شناسایی شد (نبود اسکلت Gradle، فایل‌های یتیم تکراری در ریشه‌ی پروژه که «اصلاحات» قبلی توش گم شده بودن، عدم اتصال چند-مدلی، ذخیره‌ی کلید API به‌صورت متن‌ساده). |
| **فاز ۱** | اسکلت Gradle | ساخت `settings.gradle.kts`، `gradle/libs.versions.toml`، بازنویسی `build.gradle.kts` ریشه، اضافه‌کردن پلاگین گمشده‌ی `kotlin-android`، ساخت `gradle-wrapper.properties`. |
| **فاز ۲** | پاک‌سازی | حذف ۷ فایل تکراری/یتیم از ریشه‌ی پروژه (بیرون از `app/src`) که هیچ‌وقت جزو build نبودن. |
| **فاز ۳** | چند-مدلی واقعی | در `BoardroomRepository.kt` واقعی، هر سه فراخوانی از `GeminiClient` به `ModelRouter` تغییر کرد (با پاس دادن `endpoint`). حالا انتخاب مدل هر جایگاه واقعاً به همون ارائه‌دهنده می‌ره. |
| **فاز ۴** | امنیت کلید API | `subAgentsToJson` کلید رو قبل از ذخیره در Room پاک می‌کنه؛ کلید واقعی فقط در `SecureKeyStore` (رمزنگاری‌شده) نگه‌داری می‌شه؛ `BoardroomViewModel`/`AdvisorConfigDialog`/`MainActivity` برای خوندن/نوشتن کلید از این مسیر جدید اصلاح شدن؛ `backup_rules.xml` و `data_extraction_rules.xml` واقعی طوری تنظیم شدن که دیتابیس و فایل کلیدها از بک‌آپ اندروید مستثنی باشن؛ وابستگی گمشده‌ی `androidx.security.crypto` به `app/build.gradle.kts` اضافه شد. |
| **فاز ۵** | تشخیص خودکار مقاوم | افزودن `TriageDecision` + `parseTriageDecision` (JSON ساختاریافته) در `CouncilModels.kt`؛ پرامپت تشخیص روند اصلاح شد تا مدل فقط JSON برگردونه؛ در صورت شکست parse، fallback به روش قدیمی regex محفوظ مونده. |
| **فاز ۶** | شفافیت پاسخ شبیه‌سازی‌شده | هر پاسخ placeholder در `GeminiClient.kt` (چه به‌خاطر نبود کلید، چه خطای شبکه) الان با یک بنر هشدار صریح شروع می‌شه که با پاسخ واقعی مدل قابل‌اشتباه‌گرفتن نباشه. |
| **فاز ۷** | ~~چیدمان چپ/راست~~ | **کنسل شد** — کاربر چیدمان فعلی (بند ۱.۴) رو پسندید و تأیید کرد. تغییرش ندید مگر با درخواست صریح. |

---

## ۴. کارهای باقی‌مانده (اولویت‌بندی‌شده)

1. ~~پیاده‌سازی واقعی MCP~~ — **این آیتم با کاربر بازبینی و جایگزین شد.** اتصال هر مشاور به یک مدل خاص (Claude/GPT/Ollama) از قبل در فاز ۳ به‌صورت واقعی پیاده‌سازی شده بود و ربطی به MCP نداشت. نیاز واقعی کاربر «ذخیره‌ی حافظه در GitHub یا Google Drive» بود که به‌جای MCP (که روی اندروید عملاً قابل میزبانی نیست)، با اتصال مستقیم REST به API خود GitHub/Google Drive پیاده‌سازی شد — به‌عنوان **فاز ۸** در بخش زیر مستند شده.
2. ~~تست واقعی Build~~ — **انجام و تأیید شد ✅.** از طریق GitHub Actions (ریپوی `reza0043/chart-dna`، workflow `build-apk.yml`، run id `32799271130`، Status: Success) — جزئیات در بخش ۰. اگه در آینده build جدیدی fail شد، اول لاگ همون run رو در تب Actions ریپو ببین.
3. **ساخت `gradle-wrapper.jar`** — این مورد هم عملاً حل شده، چون CI با موفقیت build گرفته (یعنی wrapper در ریپو موجود/کاربردی بوده). اگه یک کپی محلی (خارج از CI) لازم شد، با باز کردن در Android Studio یا `gradle wrapper --gradle-version 8.9` ساخته می‌شود.
4. **مهاجرت کلیدهای قدیمی** — بدون تغییر نسبت به قبل؛ فقط برای نصب‌هایی که قبل از فاز ۴ کلید واقعی وارد کرده بودن.
5. **بازبینی تست‌های موجود** — بدون تغییر نسبت به قبل؛ توجه کنید `BoardroomApp` حالا چند وابستگی جدید (`RemoteStorageCredentialStore`, `GoogleDriveAuthManager`, `RemoteSyncManager`) هم می‌سازد. چون CI موفق بوده، تست‌های واحد/Robolectric هم (اگه در همون workflow اجرا می‌شن) رد شدن؛ اگه workflow فقط assembleDebug بوده (بدون اجرای تست‌ها)، این آیتم هنوز واقعاً چک‌نشده باقی می‌مونه — لاگ workflow رو ببین تا مطمئن شی.
6. **راه‌اندازی طرف کاربر برای GitHub/Drive (فاز ۸)** — قبل از استفاده‌ی واقعی از این دو مقصد، کاربر باید مراحل زیر را خودش انجام دهد (این بخش کاری نیست که از کد قابل‌انجام باشد):
   - **GitHub**: ساخت یک Personal Access Token با scope «repo» از github.com › Settings › Developer settings، و مشخص‌کردن owner/repo/branch در تب حافظه‌ی تنظیمات اپ.
   - **Google Drive**: ساخت پروژه در [Google Cloud Console](https://console.cloud.google.com)، فعال‌سازی «Google Drive API»، ساخت OAuth Client از نوع Android (نیازمند نام پکیج `com.aistudio.aiboardroom.kxmpzq` و SHA-1 امضای دیباگ/ریلیز اپ — **نکته:** SHA-1 مربوط به کلید امضای APK که خود GitHub Actions می‌سازه ممکنه با کلید دیباگ محلی فرق کنه؛ اگه Drive روی APK دانلودشده از CI کار نکرد، اول SHA-1 همون کیستور رو که در workflow استفاده می‌شه چک کن)، و افزودن ایمیل کاربر به لیست Test users در OAuth consent screen (تا وقتی اپ در حالت «Testing» است). این مراحل نیازمند دسترسی کاربر به کنسول گوگل است و از سمت کد قابل‌انجام نیست.
7. **بررسی/به‌روزرسانی workflow (اختیاری، کم‌اهمیت)** — دو هشدار deprecation در لاگ CI هست: Node.js 20 در runnerها deprecated شده، و `actions/setup-java@v4` باید به `v5` مهاجرت کنه. غیرمسدودکننده‌ست، فقط برای تمیزی.

---

## ۵. تصمیمات و محدودیت‌های قفل‌شده (بدون هماهنگی با کاربر تغییرشون ندید)

- **چیدمان UI بند ۱.۴** — تأیید نهایی کاربره.
- **بدون فایل تکراری در ریشه‌ی پروژه** — الگوی قبلی این پروژه این بود که «اصلاحات» به‌جای اعمال‌شدن روی فایل واقعی (`app/src/main/java/...`)، در یک کپی یتیم بیرون از مسیر build نوشته می‌شدن و هیچ‌وقت واقعاً اجرا نمی‌شدن. **همیشه فقط داخل `app/src/main/java/...` ویرایش کنید و مطمئن شید فایل کپی/یتیمی جا نمونده.**
- **مدل امنیتی کلید API** — کلید هرگز نباید به‌صورت متن‌ساده وارد Room/JSON بشه؛ همیشه از `SecureKeyStore` استفاده کنید. همین قاعده برای توکن GitHub هم برقراره: همیشه از `RemoteStorageCredentialStore` (رمزنگاری‌شده، مستثنی از بک‌آپ) استفاده کنید، هرگز در Room یا SharedPreferences معمولی ذخیره نشه.
- **زبان و جهت رابط کاربری** — فارسی و RTL، در تمام رشته‌های متنی و منطق UI رعایت بشه.
- **حافظه‌ی محلی همیشه فعاله** — GitHub/Google Drive فقط «آینه»ی اضافه هستن، نه جایگزین. اگر یک روز خواستید این رفتار عوض بشه (مثلاً حافظه‌ی محلی غیرفعال بشه)، این هم باید صراحتاً از کاربر پرسیده بشه، چون روی قابلیت آفلاین‌بودن جلسات تأثیر مستقیم داره.

---

## ۶. نکات فنی تکمیلی

- استک فناوری: Kotlin, Jetpack Compose, Room (Moshi برای TypeConverterها), Retrofit + OkHttp, Coroutines, `androidx.security.crypto` (EncryptedSharedPreferences), `play-services-auth` (Identity Authorization API — فقط برای Google Drive، فاز ۸).
- ساختار داده‌ی هر مشاور (`AdvisorEntity.subAgentsJson`) از طریق `CouncilDataConverters` سریالایز/دیسریالایز می‌شه — کلید API همیشه قبل از سریالایز پاک می‌شه.
- منطق اصلی ۴ حالت دیسپچ همگی از یک تابع مشترک (`runCouncilDebateAndReport` در `BoardroomRepository.kt`) برای اجرای واقعی گفتگوی ۵‌نفره‌ی هر کارگروه استفاده می‌کنن — تغییرات باید در این تابع مشترک بمونه مگراینکه رفتار یک حالت خاص عمداً باید فرق کنه.

---

## ۷. فاز ۸ — مقصد ذخیره‌سازی انتخابی (Local / GitHub / Google Drive)

کاربر خواست حافظه‌ی شورا (پوشه‌ی master + پوشه‌ی هر جلسه) قابلیت ذخیره در GitHub یا Google Drive رو هم داشته باشه، علاوه‌بر حافظه‌ی داخلی گوشی که همیشه فعاله. طراحی نهایی:

### معماری
```
data/storage/
├── StorageBackend.kt          — enum LOCAL/GITHUB/GOOGLE_DRIVE + خطاهای اختصاصی
├── RemoteSyncTarget.kt        — قرارداد مشترک (uploadFile/downloadFile/listFiles/isConfigured)
├── GitHubSyncTarget.kt        — پیاده‌سازی برای GitHub
├── GoogleDriveSyncTarget.kt   — پیاده‌سازی برای Google Drive (resolve/cache فولدر بر اساس مسیر نسبی)
└── RemoteSyncManager.kt       — تعیین می‌کنه فعلاً کدوم مقصد فعاله و mirror رو صدا می‌زنه (best-effort،
                                  هیچ‌وقت باعث شکست ذخیره‌ی محلی نمی‌شه)
data/network/
├── GitHubSyncClient.kt        — Retrofit client برای GitHub Contents API (PAT-based)
└── GoogleDriveSyncClient.kt   — Retrofit client برای Drive API v3 (media/metadata دو-مرحله‌ای)
data/auth/
└── GoogleDriveAuthManager.kt  — Identity.getAuthorizationClient (Scope: drive.file) — نه GoogleSignIn
                                  قدیمی، نه Credential Manager؛ توکن هر بار تازه گرفته می‌شه، ذخیره نمی‌شه
data/local/
└── RemoteStorageCredentialStore.kt — مشابه SecureKeyStore، رمزنگاری‌شده و مستثنی از بک‌آپ
```

### چرا MCP نه
اجرای یک سرور MCP جدا روی اندروید عملاً امکان‌پذیر/منطقی نیست (MCP یک پردازه‌ی جدا مثل Node/Python می‌خواد). چون نیاز واقعی «ذخیره در GitHub/Drive» بود نه «دادن ابزار به یک مدل»، این با REST مستقیم حل شد — پایدارتر و بدون نیاز به زیرساخت اضافه.

### چرا drive.file scope
حداقل دسترسی ممکن: اپ فقط به فایل‌هایی که خودش در پوشه‌ی «AI Boardroom» ساخته دسترسی داره، نه به کل Drive کاربر.

### هنوز تست build واقعی نشده (بند ۴.۲ بالا) — این حجم کد جدید بیشتر از همیشه نیاز به Sync/build واقعی در Android Studio داره.

~~هنوز تست build واقعی نشده~~ — **آپدیت:** این بند تأیید شد. build کامل این فاز از طریق GitHub Actions روی ریپوی `reza0043/chart-dna` با موفقیت اجرا شد (بند ۰ و ۴.۲ رو ببین). فقط دقت کن: تست CI فقط ثابت می‌کنه کد **کامپایل** می‌شه، نه اینکه اتصال واقعی به GitHub/Drive روی گوشی واقعی هم درست کار می‌کنه — اون بخش هنوز نیاز به تست دستی بعد از انجام مراحل بند ۴.۶ داره.


---

## ۸. مسیرهای ارائه/تحویل این پروژه

- **سورس اصلی و به‌روز:** ریپوی GitHub `reza0043/chart-dna`، شاخه‌ی `main`.
- **CI/CD:** GitHub Actions، workflow با نام نمایشی «Build AI Boardroom APK» (فایل `build-apk.yml`)، روی هر push اجرا می‌شه و یک APK دیباگ به‌عنوان Artifact (و لینک دانلود مستقیم موقت از طریق tmpfiles.org در لاگ) تولید می‌کنه.
- **این سند (`HANDOFF.md`)** هم در ریشه‌ی همون ریپو نگه‌داری می‌شه — همیشه قبل از ادامه‌ی کار، آخرین نسخه‌ش رو از ریپو بخون، چون ممکنه بعد از این نسخه به‌روز شده باشه.

---

*این سند برای اولین بار همراه با کل سورس‌کد پروژه (پوشه‌ی `Ais mittings/`) در یک بسته‌ی zip تحویل داده شد؛ نسخه‌ی معتبر و زنده‌ی فعلی‌اش داخل ریپوی GitHub بالاست.*
